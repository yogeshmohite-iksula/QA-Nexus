# QA Nexus PM1 — files Prettier must NOT format
# Generated 2026-04-26

# ───── Binding docs (humans-edit-only, exact formatting matters) ─────
# 41 locked HTML frames + PRD/ERD/design system / milestone files
QA Nexus/

# ───── Generated artifacts ─────
node_modules/
.next/
dist/
out/
build/
coverage/
playwright-report/
test-results/
.turbo/

# ───── Prisma generated ─────
apps/api/src/generated/
apps/api/node_modules/.prisma/

# ───── Lockfiles + audit logs ─────
pnpm-lock.yaml
.claude/audit.jsonl
.claude/settings.local.json
# P1.14b — Claude Code transient worktrees (from /run-in-worktree etc.)
.claude/worktrees/

# ───── Model cache ─────
.cache/
**/Xenova/

# ───── Misc ─────
*.min.js
*.min.css
*.bundle.js

# ───── Design-ref HTML served via public/ ─────
# Canonical v2 HTML files vendored under apps/web/public/_design-refs/
# for direct in-browser viewing alongside React ports. They are
# byte-identical to the source frames in QA Nexus/PM1/PM1_UI_v2/ and
# must NOT be reformatted — formatting must match the canonical source
# so visual gate diffs are meaningful.
apps/web/public/_design-refs/

# ───── Untracked scratch (Day-13 (aq)) ─────
# One-shot PR body scratch files written for `gh pr create --body-file`.
# Created at repo root, never committed (caller passes them straight
# through to the GitHub API). Ignoring keeps them from tripping the
# pre-push prettier gate.
pr-*-body.md

# ───── Tooling-generated / pre-draft scratch (Day-17) ─────
# Symlinked skills sources (.agents/) — not our source-of-truth.
# Pre-draft workspace (.claude/scratch/) — in-flight ceremony docs
# (close reports, EODs, retros) before promotion to docs/. Both are
# treated as not-our-source for prettier.
.agents/
.claude/scratch/
