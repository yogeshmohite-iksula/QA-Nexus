# PR #224 review — FE+1 BUG-001 slug + BUG-005 F22 320px

> MAIN light review, Day-1 PM 2026-06-02. 18 files · +190/-103 · MERGEABLE/CLEAN.
> **Verdict: APPROVE** (no P0/P1 blockers). 2 COMMENT-level notes below.
> Merge is Wed-AM coordinator's call after Yogesh review — this review does NOT block.

## What was verified

1. **`apps/web/lib/project-slug.ts`** — new helper, 3 exports as specified (`slugFromName` · `projectFromSlug` · `getProjectStaticParams`). Well-documented: explains the BUG-001 root cause (two conflicting slug conventions — `ret` vs `iksula-returns` — under `output: export` → anchor 404'd in prod). ✅
2. **6 project-route refactors** — all of `kb / imports / upload / sources/jira (×3)` adopt `getProjectStaticParams`. ✅ No slug-routing `key.toLowerCase()` remnants in the `[slug]` route files.
3. **F22 BUG-005 RWD** — `flex-wrap` applied across DefectHeader / SherlockRca / EvidenceSection / CuratorSimilarDefects / DiscussionThread headers; evidence tab row gets `overflow-x-auto`; RightRail `dd` gets `break-words min-w-0`. ✅
4. **CHANGELOG** — 20-line entry present. **Screenshots** — both `f22-bug005-320.png` + `f22-bug005-1440.png` present. ✅

## COMMENT-level notes (non-blocking)

### C-1 — 2 residual `key.toLowerCase() === 'ret'` lookups (NOT a BUG-001 regression)

`kb-imports-page.tsx:214` + `kb-upload-page.tsx:120` still use `projects.find((p) => p.key.toLowerCase() === 'ret')?.id`. **This is project-ID resolution (find the anchor project by its key), NOT URL-slug routing** — so it's outside BUG-001's scope and correct as-is for the pilot. Flagging only as a P3 smell: hardcoded `'ret'` anchor + lingering `toLowerCase()`. Suggest a P3 follow-up to resolve the anchor project via a named constant. **No action needed for pilot.**

### C-2 — SherlockRca stack-trace: scroll-box instead of literal `break-all` (deviation-for-the-better)

Yogesh's BUG-005 ruling specified "stack-trace spans get `word-break: break-all` + `overflow-wrap: anywhere`". FE+1 instead capped the `<pre>` at column width (`min-w-0 max-w-full overflow-x-auto`, SherlockRca.tsx:172) with a BUG-005 comment. **This is better UX for stack traces** — `break-all` would shatter trace tokens mid-line (unreadable); the scroll-box preserves each line and scrolls horizontally inside a bounded box. The ruling's wrapper-`overflow-x:auto` intent IS honored. **Approve the deviation.** Yogesh's 320px visual gate is the binding check.

## Approval

**APPROVE** — clean helper, full route adoption, sound RWD, both screenshots + CHANGELOG present. C-1 + C-2 are informational; neither blocks the Wed-AM merge. Recommend merge after Yogesh's F22 320px visual-gate sign-off (per the BUG-005 ruling).

---

_MAIN light review Day-1 PM. Posted as a non-blocking PR comment on #224._
