# PR #223 review — BE+1 Day-1 baseline + NFR harnesses + embedding doc-drift

> MAIN light review, Day-1 PM 2026-06-02. 16 files · +1199/-8 · MERGEABLE/CLEAN.
> **Verdict: APPROVE** — but 1 ACTIONABLE merge-wave note + 1 FLAG for Yogesh's eyes.

## What was verified

1. **`apps/api/package.json` + `apps/web/package.json`** — **scripts only, NO new deps, NO ban-list.** New entries: `ac042:smoke`, `nfr:embed`, `seed:nfr`, `nfr:pageload` (point at the new test harnesses + scripts). ✅ Cost gate + ban-list clean.
2. **Test/script files** — `ac042-eval.ts`, `nfr-embed-latency.ts`, `nfr-fixture.seed.ts`, `scripts/ac042-smoke.mjs`, `scripts/nfr-api-latency.mjs`, `scripts/nfr-page-load.mjs` — all test/script, no production runtime. ✅ (ac042-smoke.mjs is the M5-retro carry — permanent smoke harness. Good.)
3. **Embedding doc-drift fix (CLAUDE.md + PM1_PRD + PM1_ERD)** — corrects the highest-authority docs from `bge-large/1024-dim` (plan-of-record) → `bge-small/384-dim` (actual Day-4 Render-Free-fit deployment). **All 3 citations VERIFIED to resolve:**
   - ADR-003 Day-4 amendment EXISTS (`adr-003-embedding-model.md:74` — "Render Free RAM forces bge-small for M0")
   - Migration EXISTS (`apps/api/prisma/raw/migrations/0002_vector_384_dim.sql`)
   - Code reality confirmed (`embedding-graceful.spec.ts:81` tests bge-large 470 MB refused on 512 MB dyno)
   - **This is correct housekeeping — docs aligned to deployed reality, NOT scope creep.** ✅

## 🔴 ACTIONABLE — Wed merge-wave conflict (catalogue add/add)

**Both #221 (MAIN) and #223 (BE+1) CREATE `docs/pilot-prep/2026-06-02-day-1-baseline-bugs.md`** — neither is on `origin/main` yet, so this is an **add/add conflict** at whichever PR merges second (same shape as the M5 #193/#220 memory-file conflict).

- **#221's version:** my Yogesh-rulings section (BUG-001/003/005) + P1 bucket checkboxes + verification checklist.
- **#223's version:** BE+1's +152-line baseline detail.

**Resolution at the Wed merge wave:** UNION — keep MAIN's Yogesh-rulings + P1 structure as the top, fold BE+1's baseline detail beneath (or vice-versa, whichever is structurally cleaner once both are visible). **Merge #221 first** (it has the rulings the catalogue is organized around), then resolve #223's add/add via union. Do NOT take-ours/take-theirs — both carry unique content.

## ⚠️ FLAG for Yogesh (not a block)

#223 edits **CLAUDE.md + PM1_PRD + PM1_ERD** — the HIGHEST-authority files per the conflict-resolution priority. The edits are doc-reality alignment (verified above), but merging them = an explicit "yes, the binding specs now say bge-small/384" sign-off. Worth Yogesh's eyes at merge, not a rubber-stamp. **No correctness concern from MAIN — citations all resolve.**

## Approval

**APPROVE** — clean deps, grounded doc-drift fix, all citations verified. The catalogue add/add conflict is the one thing the Wed merge wave MUST handle (union, #221-first). Flagged the binding-spec touch for Yogesh's explicit sign-off.

---

_MAIN light review Day-1 PM. Posted as a non-blocking PR comment on #223._
