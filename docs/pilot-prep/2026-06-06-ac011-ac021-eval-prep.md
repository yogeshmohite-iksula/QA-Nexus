# AC011 + AC021 eval prep (Day-3-4 Sat 2026-06-06) — Task 6

> Prep for the M5-binding Composer (AC011) + Curator (AC021) quality evals,
> drafted while the NFR-003 production measurement (Task 5) runs. The full
> harness build is **Task 6 proper (P2)** — defer to Day-29 retro if Tasks 4+5
> overrun. This doc is the runway so Day-6/Day-29 can build fast.

## What each eval measures

| eval  | agent    | measures                             | gate                     | Groq? |
| ----- | -------- | ------------------------------------ | ------------------------ | ----- |
| AC011 | Composer | generated test-case quality          | **CONFIRM from M3 spec** | YES   |
| AC021 | Curator  | duplicate-detection precision/recall | **CONFIRM from M3 spec** | NO    |

**Gate source (mandatory first step — do NOT invent thresholds):** extract the
exact AC011/AC021 acceptance thresholds from
`QA Nexus/PM1/PM1_milestone/M3/Milestone_M3_Test_Cases_AI.md` (and any M5
amendment) before running. Precedent: AC042 = top-2 ≥ 40% + calibration ≥ 0.8.

## Bootstrap pattern (reuse, don't reinvent)

- **Score/load loop:** copy `test/ac042-eval.ts` — load every golden item → run
  the agent → score → aggregate → PASS/FAIL vs gate.
- **DB wiring is the key difference:** AC042 _fakes_ Prisma because
  `orchestrator.runRca()` is pure. Composer + Curator **touch the DB**
  (assert-workspace, persist, pgvector), so use the **A1/A2 latency-harness
  bootstrap** instead (`test/nfr-a1-latency.ts` / `nfr-a2-latency.ts`): real
  `PrismaService` bound to the **test branch** (ep-blue-star) + the seeded NFR
  fixture + real Audit / Embedding / LLMGateway.
  - AC011 module: `[ComposerService, PrismaService, AuditService, LLMGatewayService]`
  - AC021 module: `[CuratorService, PrismaService, AuditService, EmbeddingService]`
  - Safety gate (verbatim from the latency harnesses): refuse unless
    `host(DATABASE_URL) === host(TEST_DATABASE_URL)` — never pilot.

## Golden sets (already exist)

- `apps/api/test/golden-sets/a1/` — Composer (AC011). **Inspect the item schema
  first** (unknown shape — read one file before drafting the scorer).
- `apps/api/test/golden-sets/a2/final/*.json` — Curator (AC021). Item shape (seen
  in `nfr-embed-latency.ts`): `{ duplicate_description, duplicate_title,
original_description, … }`. AC021 score = does `curator.check()` surface the
  known-duplicate pair above the flag threshold (and NOT false-flag originals)?

## Quota + timing

- **AC011 (Composer → Groq):** ~1 Groq call / case, **RPM-spaced ≥ 6 s** (free-tier
  cascade = the A1 failure mode). n cases ≈ `n × 6 s` + ~3-13 s generation each.
  e.g. n=20 → ~12-18 min wall, ~20 Groq calls.
- **AC021 (Curator → embedding + pgvector only, NO Groq):** ~150 ms/case warm.
  n=20 → a few seconds. Negligible quota.
- **Budget:** ~89 Groq RPD remaining; AC011 (~20-50 calls) fits comfortably.

## Run plan (Task 6 proper)

1. Extract AC011/AC021 gates from the M3 spec.
2. Read one `golden-sets/a1` item to learn the Composer golden schema.
3. Scaffold `test/ac011-eval.ts` + `test/ac021-eval.ts` from the A1/A2 bootstrap
   - the ac042 score loop; add `ac011:eval` / `ac021:eval` package scripts.
4. Run vs the test branch (`DATABASE_URL="$TEST_DATABASE_URL" … pnpm … ac0NN:eval`),
   RPM-spaced.
5. Report PASS/FAIL + n. **P2 — defer to Day-29 retro if time-short.**
