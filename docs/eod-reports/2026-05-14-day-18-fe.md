# EOD — Day 18 FE (2026-05-14)

**Author:** FE+1 (Claude Code)
**Window:** 09:30 IST – 21:00 IST (~11.5 hr active)
**M4 status at EOD:** **Day-1 of compressed M4 (Thu–Sat + Sun reserve).** F19 already on main from Day-17 cascade. F20 verbatim re-port PR holding. F21 parked on `wip/` per tooling decision. M4 Sat May 16 close timeline holds.
**Branches touched:**
`feature/fe-m4-f20-run-results-pattern-a` (#145, superseded) →
`feature/fe-m4-f20-canon-extract-fix` (#150, holding) →
`feature/fe-m4-f21-defects-hub-pattern-a` (#152, **closed + parked**) →
`wip/f21-pre-tooling-fix` (parking ref at `6783a08c`) →
`preview/m4-f20-f21-combined` (local-only, used to serve both routes for live verification) →
`chore/fe-lint-cleanup-day-18` (lint scope) →
`docs/fe-day-18-eod` (this report)

---

## Completed today

### Stage 1 — M4 kickoff brief + F20 Pattern A first attempt (~3 hr, 09:30–12:30)

Read the M4 kickoff brief: F20 Run Results / F21 Defects Hub / F19 Pattern B (BE-blocked). Hard Rule 16 codified (canonical-first 8-step pre-flight). Started F20 per the workflow:

1. Read `_DESIGN_RULES.md` (17 rules) + F20 v2 HTML (1361 lines) via `ctx_execute_file` → sandbox
2. Branched `feature/fe-m4-f20-run-results-pattern-a` off origin/main
3. Wrote 7 components + route shell (run-summary-bar / sherlock-rca-block / cluster-card / results-table / evidence-rail-pane / results-page / canned-data.ts) consuming locally-stubbed data
4. Captured 5-viewport sweep, opened **PR #145** ("feat(web): f20 run results pattern a scaffold")

### Stage 2 — Visual gate FAIL + Hard Rule 17 codification (~3 hr, 12:30–15:30)

Yogesh's review: **content invention violates Hard Rule 17** (newly codified this PR). Cluster titles, narratives, ev-rail tab structure (4 vs canonical 6), selected-case stack trace — all invented or paraphrased, not extracted verbatim from canonical HTML.

**Hard Rule 17 codified (this session):** every user-visible string in a frame port traces to a single `canned-data.ts` module per frame, which traces verbatim to canonical HTML. Components consume; they do NOT invent.

**F20 fix-forward executed:**

- Cut new branch `feature/fe-m4-f20-canon-extract-fix`
- Extracted F20 v2 HTML structurally via `ctx_execute_file` + `ctx_search` → saved to `/tmp/f20-canon-raw.json` (52 KB sandbox-only)
- Rewrote `canned-data.ts` (~550 LOC) with `NarrativeSegment` / `TitleSegment` / `ErrorHeadlineSegment` / `StackLine` typed segments to preserve `<b>` / `<code>` / `<span class="mono">` inline emphasis verbatim
- Rewrote 5 components to consume `F20_*` constants (no hardcoded user-visible strings)
- Captured 4 side-by-side PNGs (canonical HTML + React port @ 1440 + 320)
- **Opened PR #150** with Hard Rule 17 10-spot-check verification table

**PR #150 visual gate GREEN** per Yogesh side-by-side review. Holding for M4 close cascade. PR #145 superseded.

### Stage 3 — F21 Defects Hub Pattern A (Hard Rule 17 from start) (~2 hr, 15:30–17:30)

First M4 frame port executed with Hard Rule 17 from the START (no fix-forward needed):

1. `ctx_execute_file` on F21 v2 HTML (1441 lines) → `/tmp/f21-canon-raw.json` (70 KB sandbox)
2. Wrote `canned-data.ts` (~400 LOC) FIRST — 10 defects (D-2069..D-2104), filter strip, toolbar, sd-rail (Summary / Sherlock RCA 94% / Curator 3 matches / Activity 3 rows / Comments)
3. Then 5 React components consuming `F21_*` constants
4. Smoke 200 + side-by-side sweep
5. **Opened PR #152** with Hard Rule 17 10-spot-check table

### Stage 4 — F21 parked for tooling decision (~30 min, 17:30–18:00)

Yogesh flagged drift problem is **tooling, not effort**. Per directive:

- Created `wip/f21-pre-tooling-fix` parking branch at SHA `6783a08c`
- **Closed PR #152** with parked-for-tooling comment (work preserved on wip branch)
- Will re-port Day-20+ via new `.claude/skills/frame-port/` + `apps/web/tests/visual/` toolchain

### Stage 5 — Tonight's cleanup tasks (5 PM – 9 PM, ~4 hr)

Per Yogesh's tooling-prep brief:

- **F21 parking confirmed** at `wip/f21-pre-tooling-fix` (SHA `6783a08c`)
- **Canonical baselines captured** — 12 PNGs at `apps/web/tests/visual/canonical/{F19,F20,F21}/{320,768,1024,1440}.png` (4.6 MB total). Captured via headed Chromium + Playwright `file://` URL + `fullPage:true`. Reusable script at `apps/web/tests/visual/capture-canonical-baselines.mjs` (~50 LOC).
- **Hard Rule 17 exhaustive audit done** on F19 + F20 + F21 canned-data.ts vs source v2 HTML. Audit script at `/tmp/rule17-audit.mjs` greps every string literal (HTML-entity normalised, whitespace-collapsed; whitelist for CSS vars, segment-kind discriminators, enum keys).

  **Results** (full report at `docs/audits/2026-05-14-rule17-audit-day-18.md`):

  | Frame   | Strings | Violations                | Rate          |
  | ------- | ------- | ------------------------- | ------------- |
  | F19     | 150     | 7 (~4 false positives)    | 4.7 %         |
  | **F20** | **188** | **19** (8 real invention) | **10.1 %** ⚠️ |
  | F21     | 184     | 2 (≤1 real)               | 1.1 % ✅      |

  F20 has the most drift — 6 invented case-row titles + 2 page-wrapper strings + 2 formatting drifts. F21 is cleanest because Hard Rule 17 was applied from start.

- **9 PM tooling drop received** — `frame-port` skill v1 + Hard Rule 18 (skill-mandatory port workflow) merged to main as **PR #154** (`e5aa562`).

---

## In flight

- **PR #150** — F20 verbatim re-port, holding visual gate green for M4 close cascade
- **wip/f21-pre-tooling-fix** — F21 work parked, will re-port Day-20 via new toolchain
- **`apps/web/tests/visual/canonical/`** — 12 canonical baseline PNGs ready for BE+1's Playwright VR suite to consume as expected snapshots
- **Hard Rule 18 (NEW Day-18 9 PM)** — skill-mandatory port workflow via `frame-port` skill (PR #154 on main)

---

## Blockers

**None.** Quality issue diagnosed as tooling (not effort), tooling shipped 9 PM. Day-20 resume unblocked.

---

## Tomorrow (Day-19, 2026-05-15) — preview

- **No new frame ports today** — Day-19 is dedicated to tooling absorption + VR suite integration
- **AM (10:00 IST)** — read `frame-port` skill v1 + Hard Rule 18; review BE+1's VR suite config when it lands
- **AM** — confirm `apps/web/tests/visual/canonical/` baselines feed the VR suite as expected snapshots
- **PM** — practice re-port of F21 via new toolchain on `wip/f21-pre-tooling-fix` branch as the gold-path validation
- **EOD signal** — Day-19 status report

## Day-20 (2026-05-16) — Sat M4 close

- Re-port F19 + F20 + F21 via new toolchain (priority order based on audit drift): **F20 first** (8 real invention strings), F21 next (≤1 real), F19 last (false-positive cleanup)
- F19 Pattern B WebSocket wiring kicks off once BE+1 #148 merges
- M4 close cascade target end-of-day Sat

---

## Free-tier quota usage (Day-18)

| Resource        | Status                                                                              |
| --------------- | ----------------------------------------------------------------------------------- |
| Groq API        | not exercised (no Composer/Curator/Sherlock runs from FE)                           |
| Gemini fallback | not exercised                                                                       |
| Resend email    | not exercised                                                                       |
| Neon            | scale-to-zero, no FE-driven writes                                                  |
| Cloudflare R2   | no uploads                                                                          |
| Render          | normal (`/health` keep-alive ticking)                                               |
| GitHub Actions  | ~5 CI runs across #145 / #150 / #152; under daily quota                             |
| Playwright      | heavy LOCAL use (canonical baselines + 3 side-by-side sweeps); no quota implication |

---

## Day-18 retro snippets (for retro skill)

1. **Hard Rule 17 worked when applied from start.** F21 (1.1 % violation rate) vs F20 fix-forward (10.1 %) — the difference is whether canned-data extraction came BEFORE component code. Codify in `frame-port` skill: first artifact is always `canned-data.ts`, second is the spot-check audit, third is components.
2. **Cross-frame string leak class** — F19 audit flagged `"matches 2 prior incidents from 2026-03-14."` which is canonical F20 cluster 1 narrative. I had a docstring reference in F19 that copied an F20 example. Followup: the audit script should consider docstrings/comments separately from runtime strings (currently it lumps them).
3. **`ctx_execute_file` + `ctx_search` workflow worked perfectly for the verbatim extraction step.** Raw 1361-line HTML stayed in sandbox; only structured/searchable terms came into context. Reusable for every future port.
4. **Soft-reset overshoot recovery pattern** — when `git reset --soft origin/main` was used mistakenly, recovery via `reset --soft <orig-sha>` + `git merge origin/main` for CHANGELOG keep-both is clean. Codify in `frame-port` skill's error-recovery section.
5. **commitlint header-max-length 100** — bit me on F20 fix-forward title. Day-15 retro already noted this; should add to pre-commit hook as a warning not just commitlint check.
6. **Live URL testing > screenshots.** Yogesh's "open both in browser" verdict on F20+F21 caught nothing screenshots would (hover/click states, real interactivity). Codified into the new "ready for review" checklist.

---

End of Day-18. M4 Day-1 closes with tooling baseline ready. 🛠️
