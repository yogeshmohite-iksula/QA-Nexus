# Day-2 (Wed) BE EOD — Pilot push (2026-06-03)

- **Author:** BE+1 · **Branch:** `docs/be-day-1-pilot-push` (PR #223) · **Repo:** `-backend`
- **Headline:** NFR-003 harness work landed + **3 process-safety findings** (directUrl
  gotcha, cold-start, Singapore network-RTT). A4 is the only NFR-003 RED (ADR-024
  ratified). A1/A2 representative measurement deferred to a Render-co-located host.

---

## §1 — Completed today

- **Test branch (`ep-blue-star`) fully primed (writable fixture for latency probes):**
  migrated via `prisma db push` (non-destructive workaround for the P3018 empty-ledger
  collision) → M3 columns present → seeded `1/1/1/1/3` → **embeddings populated 3/3 ×
  384-dim** (`populate:embeddings`, REAL PASS).
- **A1 + A2 latency harnesses written** (`nfr:a1` Composer, `nfr:a2` Curator full
  `check()`) — typecheck + lint clean, with a `DATABASE_URL==TEST_DATABASE_URL` host
  safety gate (refuses pilot). Committed to PR #223 (`34708dd`).
- **ADR-024 ratified** (NFR-003 A4 pilot p95<20s / GA p95<15s) → PR #225.
- **`directUrl` gotcha** documented (`feedback_prisma_directurl_gotcha.md`, `f6ae242`).
- **Network-RTT methodology** documented in `m5-nfr-baseline.md` (`1e306af`).

## §2 — Findings (3 process-safety catches today)

- **12th — `prisma migrate` ignores `DATABASE_URL`, uses schema `directUrl` (= pilot).**
  A mis-targeted `migrate deploy` connected to pilot (inert — pilot already had the
  schema; Yogesh confirmed integrity). Fix: override `DIRECT_URL` too. Test branch then
  fixed via `db push`.
- **14th — A2's 7.1s local p95 is client→DB network RTT, not the algorithm.** A read-only
  `SELECT 1` probe = **~91ms/round-trip** (Mac → Neon Singapore). `check()` does ~4
  round-trips → ~360ms floor + ~98ms embed = the 1.5s p50. **Pilot shows identical RTT**
  (pilot re-measure wouldn't help) **and A1/A2 audit-write (ZERO-writes violation)** → not
  run. RATIFIED by Yogesh as a binding methodology note.
- **15th + 16th — revised Task F is environment-blocked:** deployed FE
  `pilot.qa-nexus.pages.dev` **does not resolve** (NFR-001 can't run) · local `.env`
  **missing `RESEND_FROM_EMAIL`, `R2_ENDPOINT`, `R2_BUCKET`** (Resend/R2 smokes can't run
  — full config is Render-only per Hard Rule 6).

## §3 — Blockers

- **A1/A2 representative latency** — only obtainable from a **Neon-co-located host
  (Render prod / one-off job)**; dev-Mac measurement adds ~91ms/round-trip. Production
  A2 estimate ≈ **~150ms (under the 500ms gate)**; the algorithm is sound.
- **A1 Groq RPM cascade** — needs ≥6s call spacing.
- **NFR-001** — needs the real deployed FE URL (or local FE + auth session).
- **Resend/R2 smokes** — need `RESEND_FROM_EMAIL` + `R2_ENDPOINT`/`R2_BUCKET` locally, or
  run from Render.

## §4 — Tomorrow (Thu Day-3)

- **A1 + A2 from a Render one-off job** (co-located, RPM-spaced) → representative numbers.
- NFR-001 once the deployed FE URL is provided; Resend/R2 smokes once config is provided
  (or run them from Render).
- AC011 (Composer) + AC021 (Curator) quality eval harnesses.

## §5 — Free-tier quota (Day-2)

- **Groq: ~89 RPD used** (A4 20-case ~80 + smoke 4 + A1's 5 fast-fails ~5). Well under 1000.
- **Neon:** storage ~9.5 MB (pilot); test-branch additions negligible. CU-hr → dashboard.
- **Resend / R2:** 0 ops (smokes blocked on config). **$0 gate intact.**

## §6 — Cross-references

- PR #223 commits: `f6ae242` (gotcha) · `34708dd` (C+D harnesses) · `1e306af` (RTT finding).
- PR #225 (ADR-024). · `docs/architecture/adr-024-nfr-003-pilot-vs-ga-gate.md`.
- `docs/pilot-prep/m5-nfr-baseline.md` (all NFRs + binding measurement methodology).
- `.claude/memory/feedback_prisma_directurl_gotcha.md`.

## §7 — Discipline

**16 reality-checks logged across the pilot-push** (12 directUrl · 13 cold-start · 14
network-RTT · 15 no-FE-URL · 16 missing-provider-config). **0 fabricated numbers · 0 pilot
writes · $0 gate intact.** Every measurement caveated to its environment.

---

_Day-2 close. NFR-003 harnesses + fixture shipped; A1/A2 representative numbers are a
Render-side Thu task (network-distance finding). A4 RED resolved for pilot via ADR-024._
