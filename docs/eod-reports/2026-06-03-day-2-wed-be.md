# Day-2 (Wed) BE EOD — Pilot push — _executed Tue 2026-06-02 evening (Option C live push)_

- **Author:** BE+1 · **Repo:** `-backend` worktree · branch `docs/be-day-1-pilot-push` (PR #223, cumulative — Day-1 + Day-2 evening; new branch avoided since #223 is unmerged)
- **Headline:** Live keys enabled real measurements. **Groq verified live**; **NFR-003 A4
  latency is a real RED** (p95 18.2s > 15s gate); A1/A2-full **blocked** by a pre-M3 test
  branch. No fabricated data.

---

## §1 — Completed (Tue evening, with Yogesh's live keys)

- **Task P — prereqs verified:** all 6 keys present + correct format; pilot vs test DB are
  **different Neon endpoints** (`ep-blue-silence` vs `ep-blue-star`).
- **Seed safety gate FIXED:** Day-1 gate blocked all `neon.tech` → would have wrongly
  refused the legitimate test branch. Now **compares hosts** + connects only to
  `TEST_DATABASE_URL` (PrismaClient `datasources` override, never the pilot URL).
  Typecheck + dry-run both green.
- **Groq LIVE ✓** — `ac042:smoke --case def-006`: 4 real Groq calls through all 4 Sherlock
  agents, 0 crashes, top-2 100% (1/1). Providers verified end-to-end.
- **NFR-003 A4 Sherlock latency (real, 20-case):** top-2 80% / cal 1.00 / 0 crashes ·
  **p50 12.9s / p95 18.2s / p99 22.3s** → **gate p95 < 15s ❌ FAIL** (tail driven by the
  gpt-oss-120b code/data agents). Real NFR finding.
- **Real seed — PARTIAL:** workspace + user + project + member + requirement seeded to the
  **test branch** (pilot untouched). Test-case upserts blocked (see §3).

## §2 — Deferred (honest, not fabricated)

- **A1 Composer + A2-full Curator latency** — blocked on the pre-M3 test branch (§3) +
  the `nfr:a1`/`nfr:a2` harnesses still need writing.
- **NFR-002 authenticated** — needs a session cookie + an `nfr:api` script alias.
- **NFR-001 page-load** — needs a running FE + auth session (skeleton ready: `nfr:pageload`).
- **Resend + R2 live round-trips** — need endpoint verification + a running API; lower
  priority, scheduled with the deployed-smoke pass.

## §3 — Blockers

1. **Test branch schema is pre-M3** — real seed hit `P2022: column 'format' does not exist`
   on the first test-case upsert. The branch lacks the M3 `format`/`gherkin`/… columns.
   **Unblock:** `prisma migrate deploy` to the test branch (needs a **direct**, non-pooler
   URL — the test endpoint is a pooler). Did not gamble on a late-night DDL migration.
2. **3 referenced scripts don't exist** — `nfr:a1`, `nfr:a2`, `nfr:api` were never written
   (Day-1 deferred them as net-new Composer/Curator/auth harnesses). Building them is Wed work.

## §4 — Tomorrow (Day-3 / real Wed AM)

- `migrate deploy` the test branch → re-seed (test cases) → populate embeddings (raw SQL) →
  write `nfr:a1`/`nfr:a2` harnesses → run A1 + A2-full.
- Investigate A4 p95 (18.2s > 15s): faster model on the slow agents, or a gate re-eval.
- NFR-001 with live FE; NFR-002-auth with a console-stub session cookie; live Resend/R2.

## §5 — Quota (Tue evening)

- **Groq: ~84 RPD** (4 smoke + 80 A4) — within the <100 budget.
- **Neon: storage 9.51 MB / 1.86%** of 512 MB; db up. **CU-hr is NOT in `/health/deep`** →
  Neon dashboard (Yogesh) for the compute-hours figure.
- GH Actions / Resend / R2: dashboards (Yogesh).

## §6 — Cross-references

- `docs/pilot-prep/m5-nfr-baseline.md` (A4 real + deferred markers).
- A4 results: `apps/api/test/golden-sets/sherlock-rca/results-2026-06-02.json`.
- Seed gate fix: `apps/api/test/seed/nfr-fixture.seed.ts`.
- Day-1 cumulative: PR #223 (5 commits).

## §7 — Lessons (Day-2)

1. **A4 p95 over budget is a genuine pre-pilot finding** — the 4-agent tail (120b on
   code/data) blows the 15s gate even though p50 (12.9s) passes. Worth surfacing now.
2. **Test-fixture DBs must track schema currency** — a stale branch silently blocks any
   fixture that touches recent columns (`P2022`). Branch-from-pilot or migrate-first.
3. **Verify-before-write paid off again (10th-class):** caught a too-broad safety gate
   (would refuse the test branch), 3 non-existent scripts, and the stale-schema blocker —
   before any of them caused a wrong write or a fabricated number.

## §8 — Token discipline

- Background-ran the 80-call A4 eval (harness-tracked, re-invoked on completion) instead of
  blocking. Bounded Bash output via `grep`/`python3` percentile calc. Explicit-path staging;
  `.env` read for values but never staged/printed.

---

_Executed Tue evening per Option C. Real data where runnable, honest blockers where not.
0 fabricated numbers · ~84 Groq RPD · pilot DB never touched._
