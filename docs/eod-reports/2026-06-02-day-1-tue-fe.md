# EOD — Day 1 Tuesday FE (2026-06-02) — Pilot-push baseline

**Author:** FE+1 (Claude Code)
**Window:** Tuesday 2026-06-02 (~6hr active)
**Context:** Pilot push Mon Jun 8. Day-1 = frontend integrity + RBAC/mobile/cross-browser baseline to catch P0/P1 before Day 2-3 F26/F27 ports.
**Baseline:** `origin/main` = `a635fff` (M5 CORE closed; F28 v2 + v1-delete + skill-retro all merged).
**Branches touched:** `chore/web-d1-baseline` (read-only verify) → `docs/fe-day-1-pilot-push` (this report + bug catalogue).

---

## §1 Completed today

All 5 verification activities run. Full detail + repro in the **bug catalogue**: `docs/pilot-prep/2026-06-02-day-1-baseline-bugs.md`.

- **A1 — Baseline gates: ✅ PASS.** `pnpm install` · `apps/web` typecheck (0 errors) · lint (0 errors / 3 pre-existing warnings) · `next build` (exit 0, static export, all routes prerendered).
- **A2 — Route render sweep (33 concrete URLs via headless Playwright): ✅ 26 clean.** 1 root-cause P1 (BUG-001), 1 info item (BUG-002). The defect-detail "500" was a wrong test id (`D-2104`); the real built id `DEF-RET-2104` → 200 (false alarm cleared).
- **A3 — RBAC: AdminGuard logic ✅ correct** (`role === 'Admin'` → redirect + no-flash placeholder). Full 4-persona matrix **not testable in FE-only Pattern A** (no BE auth, no role-switcher UI mounted, routes seed fixed users 7×Yogesh/1×Kishor/1×Akshay). Found P1 security gap (BUG-003) + P3 UX (BUG-004).
- **A4 — Mobile @320px (top 10 routes): ✅ 9/10 clean.** 1 P1 RWD overflow (BUG-005, F22). Hamburger present on all app routes; correctly absent on /sign-in (auth, no shell).
- **A5 — Cross-browser: ✅ Chromium clean (5/5).** WebKit/Firefox engines not installed in headless FE env → recommended Yogesh manual Safari 17 + Firefox 121 pass on Mac (authoritative for the pilot targets).

**No P0. Build is green. No crashes, no missing routes.**

### Bugs found

| ID      | Sev    | Title                                                                                                                  | Status                                   |
| ------- | ------ | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| BUG-001 | **P1** | Project sub-route slug-convention split (`ret` vs `iksula-returns`) — 404s in static-export prod depending on nav slug | Needs Yogesh convention ruling (Rule 11) |
| BUG-003 | **P1** | `/admin/*` RBAC is client-side only; admin HTML ships in static bundle (pre-MS0-T021)                                  | Needs Yogesh deploy-gate decision        |
| BUG-005 | **P1** | F22 Defect Detail horizontal scroll at 320px (Sherlock stack-trace spans + header action row)                          | Fix on approval (Rule-13-gated frame)    |
| BUG-002 | P3     | root `/` returns 200 empty body (likely redirect shell)                                                                | Verify intended                          |
| BUG-004 | P3     | `/home` doesn't surface `?error=admin-required` toast (Sonner now available)                                           | 1-line follow-up                         |

---

## §2 In flight

- 3 P1s catalogued, all **pre-deploy** (none block today's dev verification): BUG-001, BUG-003, BUG-005.
- WebKit/Firefox manual cross-browser pass (Yogesh, on Mac).

---

## §3 Blockers

None blocking today. **3 decisions needed from Yogesh/MAIN before pilot deploy:**

1. **BUG-001** — choose ONE app-wide project-slug convention (`ret` vs `iksula-returns`); FE+1 then aligns `generateStaticParams` + slug resolvers + AdminShell nav (a shared `lib/project-slug.ts` would prevent recurrence). Not fixed unilaterally per Hard Rule 11 (two self-consistent conventions, app-wide impact).
2. **BUG-003** — must the MS0-T021 BetterAuth server middleware land before Jun 8, or is client-only `/admin` gating acceptable for an 8-user trusted pilot? (FE can't close it — `output: export` precludes edge session checks.)
3. **BUG-005** — approve the F22 mobile-overflow fix (small, mobile-only, but touches a recently visual-gated frame).

---

## §4 Tomorrow Day-2 (Wed 2026-06-03)

- **F26 Agents v2 port** (~7-8hr) via skill v2.2 7-step + ADR-022 §5.9 PRE-STEP-3 sanity, IF the F26 v2 Claude Design bundle is available. In-place re-port pattern (F26 v1 exists).
- If Yogesh rules on BUG-001 / BUG-005 / BUG-003 → land those fixes first (each small).

---

## §5 Free-tier quota usage

- **Neon CU-hr:** 0 (FE+1 zero DB writes). **Groq / Atlassian:** unconstrained (no calls).
- **GitHub Actions:** ~8 min (CI on this EOD PR). **Cloudflare Pages:** 0 builds.
- **$0/month cost gate intact.**

---

## §6 Cross-references

- Bug catalogue: `docs/pilot-prep/2026-06-02-day-1-baseline-bugs.md`
- AdminGuard: `apps/web/components/admin/admin-guard.tsx` · CurrentUserContext (Pattern-A seed)
- BUG-001 loci: `apps/web/app/projects/[slug]/{kb,imports,upload,sources/jira[/step-2,step-3]}/page.tsx` (Convention A) vs `app/(app)/projects/[slug]/{defects,reports,results,runs}/page.tsx` (Convention B) · AdminShell `projectKeyLower`
- BUG-005 locus: F22 Defect Detail (Sherlock RCA code block + `components/defects/defect-header` action row)
- skill v2.2 · ADR-022 §5.9 · Hard Rule 11 (ask-don't-guess) · Hard Rule 12 (RWD) · `output: export` (Cloudflare Pages)

---

## §7 Day-1 PM update (post 5 PM) — P1 rulings actioned

Yogesh's 3 P1 rulings from the baseline review, all actioned tonight.

### BUG-001 FIXED — slug standardized on `iksula-returns`

- New helper `apps/web/lib/project-slug.ts` — `slugFromName` + `projectFromSlug` + `getProjectStaticParams` (single source of truth).
- 6 page routes (kb / imports / upload / sources × 3) + 5 nav call-sites standardized on the name-slug (was `ret`).
- Verified: all **11** `/projects/iksula-returns/*` routes return 200; old `/projects/ret/kb` correctly 404s. Build clean.

### BUG-005 FIXED — F22 320px overflow + visual-gate polish (4 rounds)

- `SherlockRca` `<pre>`: `min-w-0 max-w-full` — the RCA stack-trace scrolls inside its box (the 531px overflow culprit).
- `DefectHeader` restructured to the canonical `.title-row`: action cluster floats top-right so the title + description use full width and flow under it; breadcrumb→meta 10px, meta→title 6px (were ~24px); all five action boxes unified to 24px; WCAG tap floor preserved via `pointer-coarse` → 44px.
- `EvidenceSection` tabs: `flex-nowrap overflow-x-auto` → one horizontal-scroll row on mobile with the SYS-17 scrollbar visible (was wrapping to 3 rows).
- 320px scrollWidth 411 → 320 (no h-scroll); 1440px verified. **Yogesh visual gate PASS after 4 rounds.**

### BUG-003 ACCEPTED-FOR-PILOT

Client-side-only admin guard accepted per Yogesh ruling; server-side enforcement deferred to MS0-T021 (M6). Recorded as a pilot risk.

### Shipped

- Branch `fix/web-bug-001-bug-005-slug-and-f22-320px` → **PR #224** (commit `a0990e8`, 18 files). All 4 pre-push gates green (typecheck · prettier · lockfile · CHANGELOG).

### F26 Day-2 pre-stage — DEFERRED to Wed 09:00

- Optional pre-stage (extract-canned-data + extract-spec + §5.9 sanity) deferred — the evening went to the 4-round F22 visual gate. Day-2 starts at skill Step 1 → 2 → 3 (Yogesh spec approval) per plan.

### Day-1 final: all 3 P1 rulings actioned (2 fixed + 1 accepted). Pilot-MVP track GREEN.

---

**Standing down. Day-2 Wed 09:00 — F26 v2 port (skill v2.2 Steps 1→7); PR #224 merges to `main` first.** 🌙
