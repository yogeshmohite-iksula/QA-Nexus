# Day 27 Tue — BE+1 EOD report (M5 CORE close deferred to Wed Day-28)

**Date:** 2026-05-26 (Tuesday) · ~20:30 IST
**Author:** BE+1
**Spec context:** PM1_PRD v8.1 · PM1_ERD v2.1 · M5 CORE close deferred Sun → Mon → Tue → **Wed Day-28** after AC042 FAIL diagnosis tonight. Failure mode is narrow + fix-forward path identified (Yogesh ruling: Option 3, fresh-eyes AC042 fix Wed AM).

---

## Lead — AC042 binding result + M5 close gate verdict

```
AC042 RESULT: top-2 = 0.0% (gate 40%) · calibration = n/a (gate 0.8) · crashes = 0/50 · corpus n=50 · VERDICT: FAIL
```

**Failure mode: `prompt-strategy fixable`** — NOT model-cap, NOT key/quota, NOT corpus labels. Sherlock prompts + `SherlockHypothesisSchema` Zod are out of sync with what `openai/gpt-oss-120b` actually emits. All 4 agents (code/data/env/flake) failed Zod validation across the entire corpus → returned `[]` per ADR-019 §6 → orchestrator saw `0/4 OK` → `status=degraded` → 0 hypotheses → 0 top-2 hits.

M5 CORE close = **DEFERRED TO WED DAY-28 09:00 IST** (Option 3, fresh-eyes fix).

---

## 1. Completed today

- ACK posture maintained 14:00 → 19:50 IST (no Groq burn pre-GO; standing watch on 2-input gate)
- "GO AC042" received 19:50 IST; pulled `origin/main` (HEAD = `c517c8f` after PR #204 merge) to land LABELING-WORKSHEET.md
- Verified GROQ*API_KEY in `apps/api/.env` (length 56, prefix `gsk*`, mtime today 14:48 IST — rotation confirmed valid)
- Codex labels verified in worktree: dry-run = 45/45 ready, 0 blocked
- Ran `node scripts/apply-cpi-labels.mjs --promote`: 45 staged → live (corpus 5 → 50 def-NNN.json)
- Cherry-picked harness (`apps/api/test/ac042-eval.ts` + `apps/api/package.json` ac042:eval script) from `feat/api-ac042-eval-harness` PR #201 (still unmerged) into current worktree
- Ran `pnpm --filter @qa-nexus/api ac042:eval` binding: 200 Groq calls, ~60s wall, exit 0, results JSON written
- **AC042 RESULT: FAIL** at top-2 0.0% / calibration n/a / crashes 0/50 / corpus n=50
- Diagnosed failure: Sherlock prompt + Zod schema out of sync with model JSON output (all agents Zod-validation-failed, 0 hyps universally)
- Filed audit-trail PR (see In Flight) — 50 staged + 1 worksheet edit + 1 report + 1 results JSON force-added

---

## 2. In flight

- **PR #211 (`feat/be-ac042-corpus-codex-labels-be1`)** — corpus labels (Codex-assisted) + LABELING-REPORT + results-2026-05-26.json audit trail. Tagged `[HOLD for Wed AC042 PASS]`. Re-merge after Wed prompt/schema fix passes the gate.
  - **Branch-name deviation:** original brief named `feat/be-ac042-corpus-labels-codex` but that branch was claimed by MAIN agent for unrelated Day-27 MAIN EOD work (commit `bb4fe51`). Used `-be1` suffix to disambiguate.
  - **corpus.jsonl note:** original brief referenced this file but it does not exist — the harness loader reads `def-*.json` files directly. The 50 def-NNN.json files ARE the corpus; no jsonl rollup needed. Documented in the PR body + this EOD.
- **This PR (`docs/be-day-27-eod`)** — Day-27 BE EOD

---

## 3. Blockers

- **AC042 FAIL** — Sherlock prompt + `SherlockHypothesisSchema` Zod out of sync with `gpt-oss-120b` JSON output shape. All 4 agents (data/code/env/flake) fail validation → 0 hyps → status=degraded → 0 top-2 across entire corpus.
- **Evidence:** `/tmp/ac042-binding-2026-05-26.log` shows `route=primary provider=groq input_tokens~=400` on every call (Groq responding correctly — not the issue), and WARN signature `[SherlockDataService] agent.data Zod validation failed` on every agent invocation.
- **Fix-forward path** identified for Wed AM (3 options ranked by invasiveness):
  1. **Loosen `SherlockHypothesisSchema` Zod** to model's natural output shape (cheapest — likely the right move if model is emitting valid hypotheses just with shape drift)
  2. **Tighten ADR-019 prompt** with explicit JSON field-name example + schema-mirror in the system message (medium effort)
  3. **Add Groq `response_format: { type: 'json_schema' }` structured-output wrapper** in `LLMGatewayService` (most invasive but most robust — Groq supports this for `gpt-oss-120b`)
- **Sequence:** capture ONE raw Groq response with verbose logging → diff vs `SherlockHypothesisSchema` (`apps/api/src/agents/sherlock-code/schemas.ts`) → pick cheapest fix → re-run binding eval

**Provenance caveat for M5 close report §4 (per ADR-022 §5.9):** Corpus labels are **Codex-generated + Yogesh spot-checked** (6 sub-0.70 confidence cases reviewed), NOT pure-human ground truth. But this run's failure is NOT about labels — agents returned ZERO hypotheses, so the labels never reached the comparison layer. Label quality is independent of the Sherlock plumbing fix.

---

## 4. Tomorrow (Wed Day-28)

- **09:00 IST** — Yogesh sends RESTART. Pull `origin/main`, re-check state still clean (no overnight surprises)
- **09:00-09:15** — Verify GROQ_API_KEY still valid in `apps/api/.env` + LABELING-WORKSHEET.md + def-006…def-050 still on PR #211 branch
- **09:15-09:45** — Capture ONE raw Groq response with verbose logging (bypass Zod gate in one agent for diagnostic only)
- **09:45-10:15** — Diff actual output vs `SherlockHypothesisSchema`; pick cheapest fix (likely option 1: Zod loosen)
- **10:15-10:45** — Apply fix + re-run `pnpm --filter @qa-nexus/api ac042:eval` (~60-90s wall)
- **10:45** — POST `AC042 RESULT: ...` to MAIN. If PASS → Phase A merge wave fires. If FAIL again → escalate to Plan B (carry).
- **After Phase A** — open BE Day-28 EOD PR

---

## 5. Free-tier quota usage (Day-27)

- **Groq:** **200/1000 RPD** burned on FAIL eval (~20%). Headroom for Wed re-eval: ~800 RPD. Safe.
- **Neon:** ~83/100 CU-hr (no change today — eval is Groq-only, no DB writes). Carry from Sunday. Safe.
- **GitHub Actions:** minimal (~5 min — corpus PR + EOD PR pre-push hooks only; no CI fires on docs branches)
- **Atlassian Jira API:** 0/350 req/hr (no Jira sync activity today)
- **Render:** irrelevant for BE+1 standby work (no deploys)
- **Resend:** 0 emails sent
- **Cloudflare R2/Pages:** no uploads, no deploys

---

## 6. Cross-references

- **AC042 diagnostic log:** `/tmp/ac042-binding-2026-05-26.log` (200 Groq call traces + agent failures + final summary line)
- **Eval results artifact:** `apps/api/test/golden-sets/sherlock-rca/results-2026-05-26.json` (19,784 bytes, force-added in PR #211)
- **Corpus state on origin/main:** 50 def-\*.json (5 baseline + 45 Codex-labeled on PR #211 branch, pending Wed merge)
- **Sherlock schema location for Wed fix:** `apps/api/src/agents/sherlock-code/schemas.ts` (`SherlockHypothesisSchema`)
- **Provenance note for M5 close report §4:** labels = Codex-assisted, NOT pure human ground truth; per ADR-022 §5.9 reserve-allowance precedent. AC042 semantics this run = LLM-Sherlock vs LLM-Codex agreement, not vs pure-human judgment. M6+ corpus governance review item.
- **Failure provenance:** prompt/schema bridge, NOT corpus quality. Independent of label work.
- **ADR-019 §6 graceful degrade:** Sherlock UI degrades to amber "needs human review" banner when confidence <0.5 (universally true at 0 hyps). F22 already handles this — pilot users see honest "no hypothesis" state if Plan B (carry) ships.
- **Today's PRs:**
  - PR #211 — `feat/be-ac042-corpus-codex-labels-be1` — corpus labels audit trail
  - This PR — `docs/be-day-27-eod`
- **Still-open BE+1 PRs from prior days:** #201 (harness), #202 (Sun BE EOD), #206 (env-sync) — all `[HOLD]` for M5 close docs wave

---

## 7. Token-discipline notes (Day-11 codified)

- **`ctx_batch_execute`:** 1 use (resume-pulse + corpus state probe)
- **`ctx_execute`:** 3 uses (full git status diagnostic · apply-cpi-labels dry-run · apply-cpi-labels --promote · ac042:eval log tail) — eval itself ran in Bash background (60s wall exceeds ctx 30s cap; documented learning for Wed)
- **`cat`-heredoc for files >50 lines:** corpus PR commit message · corpus PR body · this EOD report (160+ lines)
- **`Read` only for files about-to-Edit:** none today (worktree-mutation-only session)
- **`prettier --write` before `git add`:** applied on EOD report
- **`pwd` check every turn:** maintained; one cross-worktree subshell anomaly caught + corrected (relative cd into sherlock-rca/ reported file missing while absolute path showed it present)
- **Branch-collision recovery (NEW lesson today):** MAIN agent claimed `feat/be-ac042-corpus-labels-codex` for unrelated work; first push tried to push my local commit there but pushed MAIN's commit instead. Recovery via `-be1` suffix branch + cherry-pick from `docs/be-day-25-sun-eod` (where my commit accidentally landed after a reflog reset I didn't notice). Wall-time cost: ~5 min recovery; would have been blocking without the explicit `gh pr list --head` + `git log origin/<branch>` cross-checks.

---

## 8. Work-log line (for MAIN to fold into `QA_Nexus_Work_Log.xlsx`)

```
BE+1 Day-27 Tue: ~14:00-20:30 IST · AC042 FAIL diagnosed (prompt/schema bridge) · PRs: #211 corpus + this EOD · ~6.5h total · M5 CORE close DEFERRED to Wed Day-28 09:00 IST per Yogesh Option 3 ruling
```
