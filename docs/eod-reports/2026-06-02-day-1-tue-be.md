# Day-1 (Tue) BE EOD — Pilot push (→ Mon Jun 8)

- **Date:** 2026-06-02 (Day-1, Tue) · BE+1
- **Focus:** Integrity checks on the M5 close-state system — catch P0/P1 before F26/F27 ports + pilot infra (Day 2-4).
- **Repo:** `origin/main` HEAD `a635fff` · worktree `-backend`.
- **Headline:** Baseline is **GREEN** — no P0/P1 found. 8 findings catalogued (all P2/P3); live-provider tests deferred (local env lacks Render-only keys, by design).

---

## §1 — Completed today

1. **Activity 1 — baseline integrity: ✅ ALL GREEN.** `pnpm install` (frozen-lock) + `@qa-nexus/api` typecheck + lint + build all EXIT 0. API boots on :3001, `Prisma connected to Postgres`. **Unit suite: 59 suites / 689 tests pass, 0 fail** (mocked — no DB/Groq).
2. **Activity 2 — integration health via `/health/deep` (zero Groq burn):** Neon DB **up** (1.86% / 9.51 MB of 512 MB), Groq configured, embedding warm (384-dim bge-small), Gemini/R2 placeholder-local (expected per Hard Rule 6). One call answered the whole "per-provider health" question.
3. **Activity 4 — RBAC: ✅ verified two ways.** (a) Runtime: unauthenticated GET on `/api/projects/:id/test-cases`, `/api/projects/:id/requirements`, `/llm/providers` all return **401** — guard fires, no bypass. (b) Unit: `project-roles.guard.spec.ts` PASS; role enum `{Admin, Lead, QAEngineer, Stakeholder}` matches PM1 ERD §3. 65 routes protected.
4. **Bug catalogue created** (MAIN's file was missing): `docs/pilot-prep/2026-06-02-day-1-baseline-bugs.md` — 8 findings + GREEN list + quota snapshot.

## §2 — In flight / deferred

- **Live provider round-trips** (Resend email, R2 upload, Gemini fallback) — deferred to a deployed-smoke pass (local `.env` lacks those keys by design; see F-4). Reversible the moment a deployed URL or local keys are provided.
- **Activity 3 — F28m1 Day-0 provider modal** — no FE component found in `apps/web/src` (F-6); pending FE+1 confirmation it exists before it can be exercised.
- **Activity 4 full 4-role matrix (live)** — needs Lead/QA/Stakeholder users seeded (Day-0 seed makes only the admin). Per-role _logic_ already unit-verified.

## §3 — Blockers

- **None P0/P1.** The only decision gate (F-4: where to run live-provider tests) does not block pilot delivery — config wiring is proven via `/health/deep`; real round-trips run against the deployed app pre-Jun-8.

## §4 — Tomorrow (Day-2)

- F26 Agents port observer + BE support if contracts needed.
- Perf baseline measurements (cold-start, `/health/deep` latency, embed warm time).
- Any P0 fixes (none queued today).
- If Yogesh provides deployed URL / local keys: run the deferred live-provider tests (F-4).

## §5 — Quota usage (Day-1 baseline)

| Provider   | Day-1                                      | Source                      |
| ---------- | ------------------------------------------ | --------------------------- |
| **Groq**   | **0 RPD** (no calls — budget preserved)    | "no Groq burn" rule honored |
| **Neon**   | 9.51 MB / 512 MB storage (**1.86%**)       | `/health/deep` ✅           |
| GH Actions | ≥100 CI runs since 2026-05-26; minutes TBD | billing API 404 → dashboard |
| Resend     | TBD                                        | dashboard (Yogesh)          |
| R2         | TBD                                        | dashboard (Yogesh)          |

Neon CU-hr, GH-Actions minutes, Resend count, R2 GB need provider dashboards.

## §6 — Cross-references

- **Bug catalogue:** `docs/pilot-prep/2026-06-02-day-1-baseline-bugs.md` (8 findings, this PR).
- **No P0-fix PRs** — baseline clean.
- Notable findings: F-1 (embedding doc-drift: docs say bge-large/1024, system runs bge-small/384 — consistent, doc-only), F-2 (`ac042:smoke` script missing), F-3 (brief health path wrong — `:3001/health/deep` not `:3000/api/health`), F-4 (live-provider env decision), F-6 (F28m1 FE gap), F-8 (inherited locked-frame deletions in worktree — cross-worktree bleed, FE-owned).

---

_BE+1 Day-1. Baseline GREEN, no P0/P1. Two calls made autonomously (skip Groq burn; defer live-provider tests) — both reversible; see catalogue "Decisions taken autonomously."_
