# Day 4 — 2026-04-30 — FE EOD (mid-day)

> **FE side only.** PM-side master EOD aggregator lives upstream — this file only captures what the FE chat shipped today.

## Completed today (FE side)

- **MS0-T030.m F12 Upload Requirements & Test Cases Modal** — locked source `PM1_UI_v2/frame  html view/F12 Upload Requirements · Test Cases.html`. Route `/projects/[slug]/upload`. 10 Pattern A deferred markers, drag-drop + browse, 50 MB max, 3 default sample files (CLAUDE.md `return_policy_v2.xlsx` + `legacy_refund_test_cases.csv` + `customer_return_flow_recording.mp4`), Web/Mobile/API/Cross-platform platform picker, Test Suite + Test Plan auto-create checkboxes, A1 enrichment toggle. `useCurrentUser()` + `useProject(slug)` per ADR-006. Visual gates: `docs/screenshots/rwd-upload-{320,1440}.png`. Committed `5c2d777` on `feature/fe-day-4-f12-uploads`.
- **MS0-T030.n F13 Imported Files List** — locked source `PM1_UI_v2/frame  html view/F13 Imported Files List.html`. Route `/projects/[slug]/imports`. 7 Pattern A deferred markers, full top bar + left rail with Imports sub-item active, breadcrumb, page header (Export-history + New-Import), 4-card stats strip (242 / 198 / 12 / 32), filter row with 5 chips + sort + Table/Compact toggle, 6-row import list with mixed statuses (in_progress / completed / pending_action / failed). Uploader names via `useTeamMember(uploaderId)` against `SEED_IDS.users.*` — locked source's "Priya S" / "Rahul K" placeholders swapped for canonical seed teammates Akshay P. / Kishor K. / Nitin G. per CLAUDE.md "Iksula data canon". Visual gates: `docs/screenshots/rwd-imports-{320,1440}.png`. Committed on `feature/fe-day-4-f12-uploads`.
- **followup (i) closure** — `feature/fe-day-4-f09-f10-seed-refactor` (PR #17 open) migrates F09 + F10 to seed contexts, closing followup (i) fully across all 5 components (F08a/b/c on PR #16 + F09/F10 on PR #17). F09: `PROJECT_VIEW_FIXTURES` + `joinProjectsWithFixtures()` helper joins seed entities with per-key view fixtures. F10: inline `ROSTER_SUGGESTIONS` array removed in favour of `buildRosterSuggestions(members, signedInUserId)` helper that maps seed `UserPublic` → display shape.

## PRs opened today

| #   | Branch                                   | Title                                                                              | Status                     |
| --- | ---------------------------------------- | ---------------------------------------------------------------------------------- | -------------------------- |
| #17 | `feature/fe-day-4-f09-f10-seed-refactor` | refactor(web): F09 + F10 seed-centralization (closes followup i)                   | open, awaiting MAIN review |
| TBD | `feature/fe-day-4-f12-uploads`           | feat(web): F12 Create-Project modal + F13 Imported Files list (Pattern A, ADR-006) | being opened               |

## Architectural compliance

- **ADR-006 hooks used today**: `useCurrentUser` (F12, F13), `useProject` (F12, F13), `useProjectList` (F13), `useTeamMember` (F13). No new entity-identity arrays in any local file.
- **Pattern A deferred-marker count**: F12 = 10, F13 = 7 = 17 new markers today. ZERO fetch / useMutation / axios in any new code.
- **Iksula data canon adherence**: real teammates (8-user seed roster), real sample files (CLAUDE.md upload-demo trio), RET sprint 42 day 9 references throughout.

## Hooks + RWD audit

- `enforce-design-tokens.sh` ✅ green on every commit
- `enforce-rwd.sh` ✅ green on every commit
- `enforce-pm1-stack.sh` ✅ green on every commit
- `pnpm tsc --noEmit` clean across web + api + shared
- `pnpm lint` 0 errors / 0 warnings on apps/web

## Tomorrow / next session

- Land PR #17 (F09 + F10 seed-centralization) and the F12 + F13 PR — both pending MAIN review.
- Next frame ports per M0 backlog continue from F14 onward.
