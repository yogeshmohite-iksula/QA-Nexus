# EOD — Day 4 (2026-04-30)

**Operating window:** 10 AM – 7 PM IST (~9 hrs).
**Mode:** **deploy day** — Yogesh's 6-step dashboard provisioning + 5-hotfix BE boot-regression chain + 2 FE PRs merged. 4-chat parallel coordination (PM/Cowork + MAIN + FE + BE).
**Headline:** **🟢 Render fully live**, all subsystems UP or intentionally deferred. **6 dashboard services provisioned** (Render → R2 → Resend → UptimeRobot → Grafana Cloud → Better Stack). **5 BE hotfixes** resolved a cascading boot-crash chain (NestOtelLogger → graceful boot → pnpm dup-key → sharp 0.33 → embedding memory guard). **2 PRs merged** (#17 F09+F10 seed-centralization, #19 F12+F13 uploads). **Followups (i) and (j) closed**, **(k) dropped** per Yogesh, **(l) and (m) filed**. Total infra cost still **$0/month** ✓ (Hard Rule 1 held).

---

## Completed today

### Block A — BE boot-regression hotfix chain (5 commits to main, all by MAIN chat)

| #   | SHA       | Diagnosis                                                                                                                                                                         | Fix                                                                                                                                                                                                                                                                       |
| --- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `c5eb339` | `NestOtelLogger` extended `Logger` — Nest 10's `app.useLogger(...)` requires `ConsoleLogger` subclass; bootstrap threw at `overrideLogger()` step                                 | `extends ConsoleLogger` instead of `Logger`                                                                                                                                                                                                                               |
| 2   | `4acaf4a` | `LLMGatewayService` + `EmbeddingService` threw at constructor when env vars unset → blocked Render's first deploy from passing /health                                            | Both services now boot in `deferred` state when deps unavailable; surface in /health JSON                                                                                                                                                                                 |
| 3   | `852ea72` | `package.json` had **two `pnpm` keys** (one `onlyBuiltDependencies`, one `overrides`) — JSON last-key-wins silently nuked the allowlist; sharp postinstall never fired on Render  | Merged into single `pnpm` block; smoke test added to catch future regression                                                                                                                                                                                              |
| 4   | `df7bd76` | sharp 0.32.6 + pnpm 8.x install-script policy = sharp's prebuilt-binary download blocked even with corrected allowlist                                                            | Pinned `sharp ^0.33.5` via `pnpm.overrides` (decoupled prebuilt binaries — sibling package, no postinstall needed). ADR-009 documents full diagnostic chain.                                                                                                              |
| 5   | `abf606b` | `bge-large-en-v1.5` (470 MB) + Nest baseline (~80 MB) + Prisma + R2 client > 512 MB Render Free → OOM crash loop (Render Events showed 7+ instance failures, kicked every ~2 min) | `EmbeddingService.checkMemoryHeadroom()` pre-flight guard refuses to load when `MODEL_MEMORY_MB[modelId] > heapLimit × 0.7`. Yogesh swapped `EMBEDDING_MODEL_ID` env var → `bge-small-en-v1.5` (33 MB). ADR-003 amended. 84/84 unit tests green (was 81; +3 guard tests). |

**Net result:** `https://qa-nexus-api.onrender.com/health` returns HTTP 200 with **db=up, r2=up, embedding=up (warm, model loaded in 13.3s), llm=deferred (intentional, F26 wires later), otel=deferred (intentional, awaits SDK code wiring)**.

### Block B — FE PRs merged (FE chat → MAIN chat)

| PR      | SHA       | Scope                                                                                                                                                                                   | Closes                                                |
| ------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| **#17** | `22927a5` | F09 Projects List + F10 Create Project Modal — seed-centralization migration (uses `useCurrentUser`, `useActiveProject`, `useTeamRoster` instead of inline `data.ts` placeholder names) | **Followup (i)** — 5/5 components migrated end-to-end |
| **#19** | `051313c` | F12 Upload Requirements Modal + F13 Imported Files List — Pattern A (10 + 7 deferred markers, ZERO fetch/useMutation/axios), ADR-006 hooks, Iksula data canon enforcement               | M0 backlog: T030.m + T030.n                           |

PR #19 reconciled via fresh-cherry-pick (skipped FE's docs commit because main had MAIN's many hotfix CHANGELOG entries; FE's F12/F13 entries captured in this EOD). 4-gate review (scope, Pattern A, ADR-006 hooks, RWD screenshots) all green before merge.

### Block C — Dashboard provisioning (Yogesh — 6 services in one afternoon)

All 6 wired with $0/month free-tier compliance. Total Render env vars: **14** (DB×2, BetterAuth×2, NODE_ENV, EMBEDDING_MODEL_ID, R2×4, Resend×2, OTel×4 — RESEND_API_KEY + R2 secrets gitleaks-allowlisted, never committed to repo).

| #   | Service           | Step          | Outcome                                                                                                                                                                                                                                                                                                                               |
| --- | ----------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Render**        | T011          | API live (`qa-nexus-api.onrender.com`), Singapore region, Free tier, 14 env vars, Build Command updated to include `pnpm rebuild sharp`, Health Check Path `/health`, Auto-Deploy on commit.                                                                                                                                          |
| 2   | **Cloudflare R2** | T013          | Bucket `qa-nexus-evidence-pm1` (Asia-Pacific automatic), CORS policy applied (qa-nexus-web.pages.dev + localhost:3000), scoped Account API token (Object Read & Write, qa-nexus-evidence-pm1 only, TTL Forever), $1 Cloudflare spending alert configured.                                                                             |
| 3   | **Resend**        | T014          | API key generated (`qa-nexus-pm1-api`, sending access). Using free test sender `onboarding@resend.dev` for pilot — DNS-verified Iksula domain deferred to M1 invite-flow ship.                                                                                                                                                        |
| 4   | **UptimeRobot**   | T015          | 5-min HTTP monitor on `/health`. Prevents Render Free spin-down during 10 AM – 10 PM IST pilot operating window.                                                                                                                                                                                                                      |
| 5   | **Grafana Cloud** | T019 (part 1) | OTel destination provisioned in `prod-ap-south-1` region. Token scoped to `qa-nexus-pm1-otel`. 4 Render env vars wired (`OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf`, `OTEL_EXPORTER_OTLP_HEADERS`, `OTEL_SERVICE_NAME`). Awaits MAIN's OTel SDK wiring in `apps/api/src/observability/otel.config.ts`. |
| 6   | **Better Stack**  | T019 (part 2) | Free tier locked. Slack + Grafana integrations selected. Source-token wire deferred until MAIN ships error-pattern alerting code.                                                                                                                                                                                                     |

### Block D — Architecture decision records (MAIN)

- **`docs/architecture/adr-009-pnpm-sharp-render-deploy.md`** — diagnostic chain capturing why each of the 4 hotfixes was real-but-insufficient. Documents the dup-key footgun + sharp 0.32 vs 0.33 prebuilt-binary distinction + pnpm 8 install-script policy. Guards against future contributors splitting the `pnpm` block again.
- **`docs/architecture/adr-003-embedding-model.md`** (amendment) — Day-4 RAM constraint section + model-swap decision (`bge-small-en-v1.5`) + −2.06 MTEB quality cost acknowledgment + 4-option escalation path (keep small / move to base / offload via Cloudflare Workers AI / Render dyno upgrade requires Hard Rule 1 ADR + Yogesh approval) + schema migration note (`vector(1024) → vector(384)` needs Prisma migration before A1 Scribe writes embeddings in M3).

### Block E — Followup ledger updates (PM/Cowork chat)

| ID      | Status before                  | Status after                | Notes                                                                                                                                                                            |
| ------- | ------------------------------ | --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **(i)** | OPEN                           | **CLOSED**                  | F09 + F10 migrated in PR #17; full follow-up closure (5/5 components).                                                                                                           |
| **(j)** | OPEN                           | **CLOSED**                  | CI now triggers on push-to-main (Day-4 morning, 10-min fix).                                                                                                                     |
| **(k)** | OPEN (NVIDIA Build evaluation) | **DROPPED**                 | Yogesh explicitly: "don't think or add anything about this in our PM1." Out of PM1 scope.                                                                                        |
| **(l)** | —                              | **FILED** (Day-4 afternoon) | M3 strategic eval: bge-small vs bge-large at A1 Scribe retrieval time. 4-option escalation path. Owner: BE + Yogesh + Akshay.                                                    |
| **(m)** | —                              | **FILED** (Day-4 evening)   | M1 user-facing: R2 free-tier quota alert system — admin-only top-strip banner at 50/60/70/80/90/100% thresholds. BE cron + FE banner + 1-table schema migration. Owner: BE + FE. |

---

## In flight

- **MAIN — OTel SDK wiring (`apps/api/src/observability/otel.config.ts`):** Grafana Cloud destination is provisioned with valid env vars on Render, but the API isn't yet exporting traces/logs. Once MAIN reads the env vars in OTel config and adds the trace/metric/log providers, `/health` will flip `otel.traces.exporter` and `otel.logs.exporter` from `deferred` to `up`. Estimated: ~3-4 hrs.
- **MAIN — schema migration for embedding dim change (1024 → 384):** Prisma schema currently declares `embedding vector(1024)` for the bge-large model. Now using bge-small (384-dim). Non-blocking for M0 (no embeddings written yet — table empty). Migration needs to land BEFORE A1 Scribe persists in M3.
- **MAIN — Day-4 EOD (BE-side detail):** MAIN's own EOD report capturing the 5-hotfix detailed timeline + ADR-009 + smoke test mechanics. This master EOD aggregates the cross-chat picture; MAIN's EOD is the BE-deep-dive.
- **PM/Cowork — Excel work-log consolidation:** Merge `Token_Savings_Log.xlsx` + `QA_Nexus_Work_Hours_Log.xlsx` into single master at `docs/observability/QA_Nexus_Work_Log.xlsx` with Day-3 sessions. **Followed by deletion of old `Token_Savings_Log.xlsx`.** Owner: PM/Cowork — deferred to Day-5 morning if EOD time runs out.
- **PM/Cowork — `scripts/update-work-log.py` aggregator hook:** Auto-roll session data into the consolidated Excel. Owner: PM/Cowork. Deferred to Day-5 morning.

---

## Blockers

**None.** Every track converged. R2 + DB + embedding all green; FE PRs merged; followups all in their right state.

**Watch items (not blockers):**

- **Embedding model warm-up cost:** First `/embedding/cosine` or `/embedding/test` call after a Render cold start = 13.3s for bge-small-en-v1.5 to load into memory. Fine for the M3 use-case (one warm-up per UptimeRobot ping cycle), but a regression watch item if first-call latency starts breaching the M3 NFR.
- **OTel still deferred:** /health shows `otel.traces.exporter: "deferred"` and `otel.logs.exporter: "deferred"`. Grafana Cloud is provisioned but API isn't exporting yet. Tracked in the In-flight list.
- **R2 secret in conversation history:** During R2 token capture, the secret was pasted to Cowork chat for env-var wiring. Followed best-practice doc: **rotate the R2 token after PM1 demo**. Generate fresh token, update Render env, revoke old token. Tracked as Day-5 cleanup step. (Same applies to `QA_Nexus` user token from Apr 26 — Admin Read & Write / All buckets — too broad for production; revoke after rotation.)

---

## Tomorrow (Day 5 — 2026-04-31, Friday)

| #   | Task                                                                                                          | Owner     | Effort | Notes                                                                                                                                                                                         |
| --- | ------------------------------------------------------------------------------------------------------------- | --------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | OTel SDK wiring in `apps/api/src/observability/otel.config.ts`                                                | MAIN      | 3-4h   | Read 4 OTel env vars, instantiate trace/metric/log providers, add `@nestjs/instrumentation`. Acceptance: /health flips `otel.traces.exporter` to `up` + first trace visible in Grafana Cloud. |
| 2   | Better Stack source-token + Slack alert pipeline                                                              | MAIN      | 1-2h   | Wire log shipping (Render → BetterStack via OTel collector OR direct HTTP) + first Slack alert on /health failure.                                                                            |
| 3   | Schema migration: `embedding vector(1024) → vector(384)`                                                      | BE        | 30m    | Pure DDL, no data lost (table empty). Land before A1 Scribe wires real persistence in M3.                                                                                                     |
| 4   | R2 + general security cleanup: rotate R2 Account API token + revoke `QA_Nexus` User API token                 | Yogesh    | 15m    | Hygiene step. Token rotation runbook in `docs/deploy/r2-runbook.md`.                                                                                                                          |
| 5   | PM/Cowork Excel consolidation + delete old `Token_Savings_Log.xlsx` + `scripts/update-work-log.py` aggregator | PM/Cowork | 2h     | Carry-over from Day 4.                                                                                                                                                                        |
| 6   | M0 acceptance gate dry-run (read-only audit, NOT the gate itself — that's Day 10)                             | MAIN      | 1h     | Verify all 19 acceptance criteria are testable and have evidence. Surface gaps NOW before Day 10.                                                                                             |

---

## Free-tier quota usage

| Provider         | Used                               | Limit         | Headroom | Notes                                                                                       |
| ---------------- | ---------------------------------- | ------------- | -------- | ------------------------------------------------------------------------------------------- |
| Cloudflare Pages | ~5 deploys (cumulative)            | 500/mo        | 99.0%    | Direct Upload mode unchanged.                                                               |
| GitHub Actions   | ~80 min (cumulative)               | 2,000/mo      | 96.0%    | 2 PR merges (#17, #19) + 5 BE hotfix pushes + 1 docs commit; CI runs on each.               |
| Neon             | 9.16 MB / 0.5 GB                   | 0.5 GB        | 98.2%    | Schema-only state (no real data yet).                                                       |
| Render           | ~1.5 hrs of 750/mo                 | 750h/mo       | 99.8%    | First deploy day. UptimeRobot will keep alive ~12 hrs/day → ~360 hrs/mo target — under cap. |
| Resend           | 0 / 3000/mo                        | 3000/mo       | 100%     | API key wired but no email sent yet. M1 invite-flow will start using.                       |
| Cloudflare R2    | 0 GB / 10 GB-mo                    | 10 GB-mo      | 100%     | Bucket created, no objects yet. Followup (m) will alert at 50/60/70/80/90/100%.             |
| Grafana Cloud    | 0 metrics / 10k                    | 10k           | 100%     | Provisioned, awaits MAIN's OTel SDK wire.                                                   |
| Better Stack     | 0 monitors / 10 + 0 GB / 3 GB logs | per free tier | 100%     | Provisioned, awaits MAIN's source-token wire.                                               |
| UptimeRobot      | 1 monitor / 50                     | 50            | 98%      | `/health` 5-min ping live.                                                                  |
| Groq             | 0 / 1k RPD                         | 1k RPD        | 100%     | API key NOT in Render env (intentional, F26 UI-driven config per ADR architecture).         |
| Gemini           | 0 / 1.5k RPD                       | 1.5k RPD      | 100%     | Same as Groq.                                                                               |

**Total infra cost: $0 / month** ✓ (kickoff Hard Rule #1 — held).

---

## M0 progress snapshot

- **Total tasks:** 35 (T001 → T031 + T032 R3 add-on + T033 hook hardening + T034 RWD enforcement)
- **Confirmed code-side closed today (delta from Day-3):** +2 (T030.m F12 via PR #19, T030.n F13 via PR #19)
- **Provisioned today by Yogesh (delta from Day-3 "runbook-ready" state):** +5 (T011 Render, T013 R2, T014 Resend, T015 UptimeRobot, T019 Grafana + Better Stack)
- **Total at Day-4 close:** **~36+ confirmed of 35 listed** (numbering drift acknowledged — STATUS.md will reconcile in Day-5 morning audit. Day-3 "32 + 4 runbook-ready" overlap with today's provisioning means net new closures need disambiguation.)
- **Acceptance gates passed:** **0 / 19** (gates run on Day 10 per M0 §4)
- **Days elapsed:** **4 of 10** (40% time elapsed)
- **Estimated burn:** **~290h of 298h ≈ 97% hours burned at 40% time elapsed**
- **Burn-rate signal:** **🟢🟢🟢 ahead by ~57 pp** — M0 substantially ahead. Remainder is acceptance-gate dry-runs + final code closures.

---

## Architecture-decision events today

- **ADR-003 amendment** — embedding model swap (bge-large → bge-small) under Render Free RAM constraint. Documents 4-option future escalation.
- **ADR-009 created** — pnpm/sharp/Render deployment chain. Diagnostic record so future contributors don't re-litigate.

## Skill conformance audit

**Deferred to Day-5 morning** (after M0 task-count audit reconciles the numbering drift). Per `docs/audits/2026-04-27-skill-alignment-audit.md` cadence ("after big structural changes — milestone close, 5+ skill-scope commits"), today qualifies on commit volume but not on milestone-close. Audit can run with M0 acceptance-gate dry-run (Day 5 task #6 above).

---

## Cross-chat summary

| Chat          | Output                                                                                                                                                                    | Net commits to main                                                |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| **PM/Cowork** | This EOD, followup (m), dashboard provisioning coordination, Yogesh's hand-holding through 6 services                                                                     | 1 (this EOD; Excel consolidation deferred to Day-5)                |
| **MAIN**      | 5 hotfixes, ADR-009, ADR-003 amendment, PR #17 + PR #19 reconcile + merge, smoke test, memory guard                                                                       | 5+ (the hotfix chain) plus PR-merge commits                        |
| **FE**        | F12 (Upload Modal) + F13 (Imported Files List) ports — 5 components, 4 RWD screenshots, 17 deferred Pattern A markers, 4 ADR-006 hooks. Reconciled via fresh-cherry-pick. | 0 (delivered via PR #17 + PR #19 merges)                           |
| **BE**        | Standing by for OTel SDK wire (Day 5 task #1) and schema migration (Day 5 task #3)                                                                                        | 0 (no BE work needed today; F26 LLM provider wiring is M1, not M0) |

---

🎯 **Day 4 is closed.** **🟢 Production-shaped infrastructure is alive.** Day 5 opens to instrument it (OTel) and start using it (M0 acceptance gates dry-run + final closures).
