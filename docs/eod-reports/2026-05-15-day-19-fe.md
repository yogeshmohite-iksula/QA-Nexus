# EOD — Day 19 FE (2026-05-15)

**Author:** FE+1 (Claude Code)
**Window:** 08:00 IST – 17:00 IST (~9 hr active)
**M4 status at EOD:** **Day-2 of compressed M4.** Tooling layer hardened; first practice port (F21) shipped through new `frame-port` skill workflow. M4 Sat May 16 close timeline holds.
**Branches touched:**
`feature/fe-vr-baseline-seed` (#156, **MERGED**) →
`feature/fe-f21-replatform-via-skill` (#160, **READY-FOR-REVIEW**) →
`docs/fe-day-19-eod` (this report)

---

## Completed today

### Stage 1 — VR baseline seed PR (~1 hr, 08:00–09:00)

**PR [#156](https://github.com/yogeshmohite-iksula/QA-Nexus/pull/156) — feat(visual-regression): seed canonical baselines for F19/F20/F21 — MERGED.**

- 12 canonical baseline PNGs committed under `apps/web/tests/visual/canonical/{F19,F20,F21}/{320,768,1024,1440}.png` (4.6 MB total). Captured Day-18 evening via headed Chromium + Playwright `file://` (full-page, dark colorScheme).
- 3 frame specs written: `f19.spec.ts` (route on main, fully active) + `f20.spec.ts` (`F20_ROUTE_READY` gate, second-skip pending PR #150) + `f21.spec.ts` (`F21_ROUTE_READY` gate, pending Day-20 re-port).
- `home.spec.ts` placeholder removed; superseded by frame-specific specs.
- `VR_BASELINES_READY: "1"` activated in `.github/workflows/ci.yml` visual-regression env block.
- All 4 pre-push gates green; merged shortly after open.

### Stage 2 — F21 practice re-port via frame-port skill (~7.5 hr, 09:00–16:30)

**PR [#160](https://github.com/yogeshmohite-iksula/QA-Nexus/pull/160) — feat(web): F21 Defects Hub re-port via frame-port skill — READY-FOR-REVIEW.**

First production use of the new skill (`extract-spec.mjs` + `diff-probe.mjs` per Hard Rule 18). Surfaced + escalated 5 skill bugs through real-port iteration; each fix shipped by MAIN on PR #158 same-branch force-pushes.

**Step-by-step execution:**

1. **Step 1 (Hard Rule 17 canned-data)** — Imported Day-18 F21 canned-data (cleanest of 3 audited frames, 1.1 % violation rate). Applied Day-18 audit fix: `F21_SEARCH_PLACEHOLDER` `'Search defects, IDs, JIRA, comments…'` (invented) → `'Search by ID, title, ticket #…'` (verbatim L789).
2. **Step 2 (Hard Rule 18 extract-spec)** — `apps/web/components/defects/spec.json` generated (6.2 KB, 34 tokens, 1 top-level section, depth-5 tree, 7 data-attrs, 2 heading exemplars, 15 aria-label exemplars).
3. **Step 3 (mandatory pause)** — Posted spec.json summary to chat. Yogesh approved.
4. **Step 4 (scaffold from spec)** — Day-18 components imported as seed. **NEW** `def-head.tsx` added — Day-18 missed the def-head region entirely (canonical L729-757 with title + 6 stats + 3 right-side actions); diff-probe surfaced as Finding B. Verbatim canned-data added: title `'Defects · Refund Flow'`, 6 stat tiles (187 total / 23 P0 / 41 P1 / 14 stale >7d / 156 with RCA / 89 fixed last 30d).
5. **Step 5 (diff-probe iteration)** — 5 skill iterations validated through my re-probes:

| Skill ver           | Bug surfaced                                                               | Result                                                        |
| ------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------- |
| v1                  | Finding A (class-name vs Tailwind) + Finding B (def-head missing)          | 17.5/39/41/44 % pixel diff                                    |
| v2.1                | Bug A (asymmetric crop) + Bug B (class-match dropped)                      | Same pixel diff; UNION crop applied but rail/topbar hardcoded |
| v2.1.1              | UNION crop + SECONDARY tier — works for measurement; pixel-diff still high | Same pixel diff (44-47 % at large viewports)                  |
| v2.1.1 → escalation | **Bug C: pixel-diff math without AA tolerance** (Case C posted)            | MAIN iterates                                                 |
| **v2.1.2**          | pixelmatch + `includeAA: false` + `report.json pixelDiff` fix              | **5.2-6.6 % range — 10x improvement**                         |

6. **Step 6 (Hard Rule 13 visual gate)** — Screenshots committed at `cd1286d`: `docs/visual-gates/F21-v2.1.2-{1440,320}.png` (293 KB + 24 KB).
7. **PR ready-for-review** — Comment posted with full v2.1.2 results table + Hard Rule 17 audit trail + skill iteration debugging contributions. State: OPEN · MERGEABLE · UNSTABLE (CI).

**Result: Diff-probe AMBER (5.2-6.6 %).** Pragmatic ceiling per Yogesh's call — the 0.2-1.6 pts above 5 % is renderer-noise floor (sub-pixel layout jitter + lucide-vs-inline-SVG variance). Hard Rule 18 amendment Part 3 (band system) codifies AMBER acceptance.

### Stage 3 — Brief recovery moments

- **node_modules cascade fail (~30 min, 10:00 AM)** — `pnpm install --frozen-lockfile` + nuclear `mv node_modules` + retry both showed same `@hookform/resolvers` ↔ zod typecheck failure. Diagnostic via `pnpm --filter web why zod` + `readlink` posted to chat. PR #159 from BE+1 unblocked it.
- **Coordinator-vs-actual desync (~25 min)** — Three messages claimed `#155/#157/#159` merged when `gh pr view <n>` returned `state: OPEN, mergedAt: null` on each. Held push per "DO NOT work around" rule; reported factual mismatch each time. Real merge happened around third message — push then succeeded clean.
- **Accidental skill-overlay commit (~10 min)** — `git checkout origin/docs/main-skill-v1-polish -- .claude/skills/frame-port/` auto-stages files; my first commit captured 4 skill files (487 lines) alongside the 2 TSX files (2 lines). Caught via `git show --stat`, recovered via `git reset --soft HEAD~1` + `git restore --staged .claude/skills/` + re-commit. PR diff stays clean (TSX + canned-data + spec.json only).

---

## In flight

- **PR #160** — F21 ready-for-review, awaiting Yogesh visual gate
- **PR #158** (MAIN) — diff-probe v2.1.2 + Hard Rule 18 amendment Part 3 — HOLD pending my F21 validation; should be ready to lift now
- **Skill overlay live in working tree of F21 branch (NOT committed)** — v2.1.2 active; reverted from F21 branch tip via `git checkout HEAD -- .claude/skills/frame-port/` before push, so PR diff stays TSX-only

---

## Blockers

**None.**

---

## Tomorrow (Day-20, 2026-05-16) — Sat M4 close day

Per Yogesh's priority order from Hard Rule 17 audit Day-18:

1. **F20** (10.1 % violation rate, highest cleanup — skill will catch drift early so should converge fast)
2. ~~F21~~ (done today)
3. **F19** (mostly false positives — fastest validation)
4. **F22 Defect Detail** (new ground, Pattern A scaffold)

Estimated: 2 ports per day Day-20 + Day-21 = all 4 done by Day-21 EOD. M4 close cascade target end-of-day Sat.

---

## Free-tier quota usage (Day-19)

| Resource       | Status                                                                          |
| -------------- | ------------------------------------------------------------------------------- |
| Groq / Gemini  | not exercised                                                                   |
| Resend email   | not exercised                                                                   |
| Neon           | scale-to-zero                                                                   |
| Cloudflare R2  | no uploads                                                                      |
| Render         | normal (`/health` ticking)                                                      |
| GitHub Actions | ~6 CI runs across #156 / #160                                                   |
| Playwright     | heavy LOCAL use (5 diff-probe runs across skill versions); no quota implication |

---

## Day-19 retro snippets (for retro skill)

1. **Skill iteration loop worked exactly as designed.** Each bug surfaced through real-port use → escalated → MAIN iterated → re-overlay + re-probe. 5 skill bugs caught in 1 day by 1 frame port. F19/F20/F22 Day-20 ports will benefit from a much-more-hardened skill.
2. **`git checkout <ref> -- <path>` auto-stages files.** Trap. Always check `git status --short` before `git commit` on overlay-pulled work. Hard Rule 11 "trust but verify" discipline.
3. **Hard Rule 18 amendment Part 3 (band system) is the right call.** Pure 5 % threshold is unrealistic for cross-renderer (raw HTML vs Next.js SSR) ports. AMBER (5-10 %) + Yogesh-eye visual gate together = robust review without chasing renderer-noise floor.
4. **Coordinator-vs-actual chat-state desync** happened 3 times in 1 day. Real merge confirmation requires `gh pr view <n>` + `git ls-remote origin refs/heads/main` cross-check. Saved CI runs by holding pushes until both authorities agreed.
5. **The pixelmatch root devDep** wasn't on origin/main when v2.1.2 overlay loaded — `pnpm add -w -D pixelmatch@^7.2.0` local-only fixed it; reverted root `package.json` + lockfile before push so F21 PR diff stays clean. Suggests skill v2.1.2 should ship the dep on its `docs/main-skill-v1-polish` branch alongside the code (it does — but the dep doesn't reach FE branches until #158 merges).

---

End of Day-19. Tooling layer hardened, F21 practice port complete. 🛠️
