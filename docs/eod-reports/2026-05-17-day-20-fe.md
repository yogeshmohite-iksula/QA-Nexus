# EOD — Day 20 FE (2026-05-17)

**Author:** FE+1 (Claude Code)
**Window:** 15:00 IST – 18:00 IST (~3 hr active, post-context-resume)
**M4 status at EOD:** Day-3 of compressed M4. F20 PR #169 visual gate PASSED. M4 close cascade merge wave in progress; Day-21 = F19 + F22 + Kimi HIGH triage.
**Branches touched:**
`feature/fe-f20-replatform-via-skill` (#169, **PASSED** awaiting merge) →
`docs/fe-day-20-eod` (this report)

---

## Completed today

### F20 Run Results — visual gate PASSED in 7 rounds (`1970a8a`)

**PR [#169](https://github.com/yogeshmohite-iksula/QA-Nexus/pull/169) — feat(web): F20 Run Results re-port via frame-port skill v2.1.2 — VISUAL GATE PASSED at 18:02 IST.**

Second production use of the frame-port skill (after F21 Day-19). Day-19 lessons (data-canonical-section attrs, bg hierarchy, mobile RWD patterns, screenshot fullPage:2400px) transferred cleanly. 7 rounds of visual gate iteration vs F21's 5 rounds — slightly more because F20 has more sections (cluster cards × 3, results table with suites, ev-rail with 5 sub-sections).

#### 7-step skill workflow execution

| Step                       | Outcome                                                                                                                                                                                                                                                                                |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 Branch                   | `feature/fe-f20-replatform-via-skill` from origin/main (post #160 F21 merge)                                                                                                                                                                                                           |
| 2 extract-canned-data      | `apps/web/components/results/canned-data.ts` — semantic re-org from auto-extract (RunStat/Cluster/ResultsSuite/EvTab + 30+ exports)                                                                                                                                                    |
| 3 extract-spec             | `apps/web/components/results/spec.json` — schemaVersion 2, 19 structural sections, 34 tokens, 18 aria-label exemplars                                                                                                                                                                  |
| 4 Yogesh approval          | ✓ at ~15:10 IST                                                                                                                                                                                                                                                                        |
| 5 TSX scaffold             | 7 components + route: results-page, run-summary, sherlock-block, cluster-card, results-table, ev-rail, action-footer                                                                                                                                                                   |
| 6 diff-probe v2.1.2        | DOM 15/17 PASS (88% PRIMARY tier — above AMBER 60%); pixel 9.4-12.8% (AMBER on 1440, borderline RED on small viewports — Hard Rule 18 amendment Part 3 band system). 2 MISSING via SECONDARY class-match (rail-content + rail-foot AdminShell-internal — known F21 v2.2 gap, accepted) |
| 7 Hard Rule 13 screenshots | `docs/visual-gates/F20-v2.1.2-{1440,320}.png` (fullPage 2400px viewport per F21 R2 fix)                                                                                                                                                                                                |

#### 7-round audit trail

| Round              | Commit        | Fix                                                                                                                                                                                                                        |
| ------------------ | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1 seed            | `6d54cd6`     | Initial 7-component scaffold + route + canned-data + spec.json                                                                                                                                                             |
| R1 → R2 (chore)    | `f57837f`     | prettier-fix `.claude/skills/frame-port/diff-probe.mjs` (pre-existing main formatting from PR #158 — unblocked pre-push gate)                                                                                              |
| **R2 (Yogesh F1)** | `5d49356`     | sb-conf-row + sb-actions + ev-rail Selected · TC chip                                                                                                                                                                      |
| **R3 (Yogesh F2)** | `2ac5aca`     | 7 fixes: Sherlock violet keywords + run-summary compact + results outer border + suite collapsible + count cells + sticky footer + cluster num-dot tone + ev-rail always-shots                                             |
| **R4 (Yogesh F3)** | `66412c0`     | 8 fixes: RunSummary full-width + Sherlock card with 3px stripe + cluster cl-conf mixed → RED + StackTrace in code box + max-h scroll + bigger screenshots + Selected case 3-row layout + sel-id teal + related teal/violet |
| **R5 (Yogesh F4)** | `31789f6`     | Stats horizontal-scroll (canonical L268-275) + Selected case 3-row exact layout (canonical L1099-1119)                                                                                                                     |
| **R6 (Yogesh F5)** | `ecb236c`     | Evidence rail visible on mobile (was `hidden lg:block`)                                                                                                                                                                    |
| **R7 PASSED**      | **`1970a8a`** | Filter pills moved to right side beside Sort                                                                                                                                                                               |

#### Day-18 audit recovery confirmed

5 "invented" test IDs from `docs/audits/2026-05-14-rule17-audit-day-18.md` — TC-RET-0345, TC-AUT-0094, TC-AUT-0102, TC-PAY-0218, TC-PAY-0224 — all canonical strings at L953/L993/L999/L1027/L1033. Now verbatim. **10.1% Rule 17 violation rate → 0%.**

---

## Reusable patterns from F20 (apply to F19/F22 Day-21)

### TSX patterns

**Horizontal-scroll bar with hidden scrollbar (canonical .rs-stats):**

```tsx
<span
  className="inline-flex min-w-0 flex-1 items-center overflow-x-auto"
  style={{ scrollbarWidth: 'none' }}
>
  {items.map((item, i) => (
    <span
      className="inline-flex flex-none whitespace-nowrap px-3"
      style={{ borderRight: isLast ? 'none' : '1px solid var(--border)' }}
    >
      ...
    </span>
  ))}
</span>
```

**Selected-case 3-row button layout (canonical L1099-1119):**

```tsx
<div className="mt-1 flex flex-col gap-1.5">
  <button className="h-9 w-full" variant="violet">
    + Open defect
  </button>
  <div className="grid grid-cols-2 gap-1.5">
    {' '}
    {/* Always equal 50/50 */}
    <button className="h-8" variant="secondary">
      Mark flaky
    </button>
    <button className="h-8" variant="secondary">
      Re-run case
    </button>
  </div>
  <button className="h-8 w-full" variant="tertiary-teal">
    View in Run Console →
  </button>
</div>
```

**Mobile evidence rail stacking:**

```tsx
<div className="lg:min-h-0">
  {' '}
  {/* Was: hidden lg:block — invisible on mobile */}
  <EvRail />
</div>
// + ev-rail aside: className="border-t lg:border-l lg:border-t-0"
```

**Header layout (title left, controls right):**

```tsx
<header className="flex flex-wrap items-center gap-x-3">
  <h2>Title</h2>
  <span className="min-w-2 flex-1" /> {/* Spacer BEFORE controls */}
  <div>Filter pills</div>
  <button>Sort</button>
</header>
```

**Bordered cell separators (canonical .stat / .suite-counts):**

```tsx
<span style={{ borderRight: isLast ? 'none' : '1px solid var(--border)' }}>
```

### Bg hierarchy (F20 final)

| Region                  | Bg                                          | Border                    |
| ----------------------- | ------------------------------------------- | ------------------------- |
| run-summary (top strip) | `--base`                                    | bottom                    |
| Sherlock card           | `--overlay` + 3px `--secondary` left stripe | `--ai-line`               |
| Cluster cards           | `--base` + 3px tone left stripe             | tone-colored line         |
| Results-block container | `--base`                                    | `--border` rounded-xl     |
| Suite headers           | `--canvas`                                  | `--border` bottom         |
| Evidence rail           | `--canvas`                                  | top mobile / left desktop |
| Action footer (sticky)  | `--base`                                    | top                       |

### cl-conf color matrix (canonical L371-373)

| Tone     | Bg            | Border        | Color    | Example                  |
| -------- | ------------- | ------------- | -------- | ------------------------ |
| `.high`  | `--pass-soft` | `--pass-line` | `--pass` | "87% confidence" green   |
| `.med`   | `--warn-soft` | `--warn-line` | `--warn` | "91% env-category" amber |
| `.mixed` | `--fail-soft` | `--fail-line` | `--fail` | "40-75% mixed" RED       |

Note: this is OPPOSITE of `.cl-class.mixed` which uses overlay/grey. cl-conf tracks confidence severity; cl-class tracks the bug taxonomy.

### Code-block styling (canonical .sel-error)

```tsx
<div
  className="max-h-[140px] overflow-y-auto rounded-md px-2.5 py-2 font-mono text-[10.5px]"
  style={{
    background: 'rgba(248,113,113,0.06)', // red-tinted bg
    color: 'var(--fail)',
  }}
>
  <span style={{ color: 'var(--fail)' }}>ErrorType:</span>
  <span style={{ color: 'var(--info)' }}>5000ms</span> {/* keys = info blue */}
  <span style={{ color: 'var(--t4)' }}>at File.method:line</span> {/* dim t4 */}
</div>
```

---

## 4 Rule 11 / discipline catches today

1. **Stats wrap → horizontal-scroll** (R3→R5) — Yogesh's "info utilize space, show in one line at top" caught that my R3 stats wrapped to multiple rows. Canonical L268-275 had `overflow-x:auto` + hidden scrollbar + bordered cells. R5 implemented exact.
2. **Selected case button layout** (R4→R5) — R4 used flex-wrap which wrapped Mark flaky + Re-run case onto 2 lines. Canonical L1099-1119 has explicit `display:grid;grid-template-columns:1fr 1fr;gap:6px`. R5 fixed with `grid-cols-2`.
3. **cl-conf colors inverted** (R3→R4) — R3 had Cluster 1 high=fail-red (wrong) + Cluster 3 mixed=overlay-grey (wrong). Canonical L371-373 has high=pass green / med=warn amber / mixed=fail RED. R4 fixed both.
4. **Evidence rail hidden on mobile** (R5→R6) — `hidden lg:block` made the entire Evidence section inaccessible below 1024px. R6 changed to `lg:min-h-0` so it stacks below center-pane on mobile with `border-t lg:border-l lg:border-t-0`.

---

## In flight

- **PR #169** — F20 visual gate PASSED at `1970a8a`, awaiting M4 close cascade merge
- **Skill overlay** live in working tree (not committed to F20 PR — pixelmatch dep already on origin/main from PR #158)

---

## Blockers

**None.**

---

## Tomorrow (Day-21, 2026-05-18) — M4 close cascade

Per Yogesh's priority queue:

1. **09:00 AM — F19 Run Console Pattern B re-port** (mostly false positives per Day-18 audit, fastest validation)
2. **After F19 — F22 Defect Detail Pattern A** (new ground, no Day-18 seed — clean scaffold via skill workflow)
3. **Stretch — Kimi HIGH FE triage** (4 items)

Estimated: 2-3 ports + triage by EOD Day-21. M4 close cascade target end-of-day Sun May 17 21:00 IST (reserve absorbed).

7-step skill workflow now production-validated through F21 (Day-19) + F20 (Day-20). Day-21 ports should go through the same flow:

- Step 1: extract-canned-data verbatim
- Step 2: extract-spec.mjs → spec.json
- Step 3: Yogesh approves spec
- Step 4: scaffold TSX from spec NOT HTML
- Step 5: diff-probe at 4 viewports
- Step 6: fullPage screenshots (2400px viewport)
- Step 7: tunnel for Yogesh live visual gate

If skill bug surfaces during F19/F22 — **DO NOT work around**. Pause, post finding, MAIN iterates per F21 R2 close-and-redo precedent.

---

## Free-tier quota usage (Day-20)

| Resource       | Status                                                                       |
| -------------- | ---------------------------------------------------------------------------- |
| Groq / Gemini  | not exercised                                                                |
| Resend email   | not exercised                                                                |
| Neon           | scale-to-zero                                                                |
| Cloudflare R2  | no uploads                                                                   |
| Render         | normal (/health ticking)                                                     |
| GitHub Actions | ~10 CI runs across #169 (7 visual gate rounds)                               |
| Playwright     | heavy LOCAL use (1 diff-probe + 8 screenshot captures); no quota implication |

---

## Day-20 retro snippets (for retro skill)

1. **7-round visual gate cycle is the new normal for canonical-faithful ports.** F21 = 5 rounds, F20 = 7. Each round caught a specific drift class that the diff-probe couldn't see (color shade, layout structure, mobile RWD, spacing). Hard Rule 18 Part 3 band system + Hard Rule 13 visual gate combo works as designed — diff-probe catches structural misses, Yogesh-by-eye catches presentation drift.
2. **Yogesh's "in line with f20 v2 html" framing is high-signal.** When he says "should look like canonical" he means PIXEL-MATCH the canonical (not approximate). Going through canonical CSS L-numbers line-by-line is the right discipline.
3. **Canonical-CSS-deviation-with-documentation pattern is sustainable.** Several R4-R5 decisions deviated from canonical for design-token-whitelist reasons (e.g. P1 amber instead of canonical's #F97316 orange). Documenting the deviation rationale in code comments keeps the diff-probe auditable.
4. **Skill v2.2 nested-section-count amendment still pending.** Both F21 and F20 hit the same SECONDARY-tier class-match gap for AdminShell-internal sections (rail-content, rail-foot). Worth pushing MAIN to ship the amendment so Day-21+ frames don't hit it.

---

End of Day-20. F20 visual gate PASSED. 🎯
