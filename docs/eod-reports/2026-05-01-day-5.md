# EOD — Day 5 (2026-05-01)

**Operating window:** 10 AM – 8 PM IST (~10 hrs).
**Mode:** **observability + M1 prep day** — MAIN wired OTel SDK + Better Stack Slack alert pipeline; BE delivered M0's last code task (vector dim migration) + M1 prep on feature branch; FE delivered all 3 M1 frame ports on feature branch; T032 golden-set seeds parsed (A1 + A2 ready, A4 awaits L1-L5 tagging Day 6).
**Headline:** **5 commits to main** + **4 commits across 2 feature branches** + **2 PRs opened** (#20 vector dim, #21 FE M1 prep w/ DO NOT MERGE flag). MAIN caught a real Render deploy break in (`e23d0d2`) and shipped a structural prevention via `cee7dfb` (new pre-push frozen-lockfile gate). T032 golden sets (R3 mitigation) parsed end-to-end: 30 A1 reqs + 102 A2 dup pairs + 50 A4 defects, all pulled from real CPI Jira data. AC014 path defined (Better Stack Slack alert) — STRUCTURAL PASS achieved, end-to-end fire pending Day 6. Total infra cost still **$0/month** ✓ (Hard Rule 1 held).

---

## Completed today

### Block A — MAIN: observability stack code wiring (3 commits to main)

| #   | SHA       | Scope                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `b2a6235` | **`docs(spec)`** — Two amendments to `Milestone_M0_Setup_v8.md`: (a) MS0-T017 reflects the OTel SDK wiring decision (Grafana Cloud OTLP for traces + Better Stack OTLP for logs, dual-exporter pattern); (b) AC008 reflects the Day-4 model swap (`bge-large` → `bge-small`, 1024 → 384 dim). Brings binding spec in line with Day-4 architectural decisions.                                                                                                                                                                                                                                                                                                                           |
| 2   | `68d4e0e` | **`feat(observability)`** — OTel SDK wire: `llm.complete` span instrumentation in `LLMGatewayService.complete()` (provider, model, prompt_tokens, completion_tokens, latency_ms, fallback_triggered, route_reason attrs). New `/admin/otel/test-trace` Admin-gated endpoint (refuses in prod unless `ALLOW_ADMIN_OTEL_TEST=true`). `/health` enhanced with OTel diagnostics (`otel.traces.env_present` + `otel.logs.env_present` boolean-only + admin-friendly `deferred_reason` text). 84/84 unit tests still green.                                                                                                                                                                   |
| 3   | `e23d0d2` | **`feat(observability)`** — Better Stack Slack alert pipeline (Path A per Day-5 architectural decision: Better Stack OWNS the alerting; API just emits trigger events). New `/admin/alerts/test-slack` Admin-gated endpoint (mirrors otel/test-trace pattern, emits ERROR severity OTel log with unique `test_marker` body). `OtelTestController` re-mounted at `@Controller('admin')` so both endpoints share one path. `docs/deploy/better-stack-runbook.md` §7 added: 3 named alert rules with rate-limit rationale + sample payloads + post-pilot tuning guidance. `docs/SECURITY.md` rotation table updated (separate Grafana Cloud + Better Stack OTLP rows + Slack webhook row). |

### Block B — MAIN: structural fix for the Render deploy break (`cee7dfb`)

`e23d0d2` added `@opentelemetry/api@^1.9.1` to `apps/api/package.json` without regenerating `pnpm-lock.yaml`. Render's `pnpm install --frozen-lockfile` rejected boot with `ERR_PNPM_OUTDATED_LOCKFILE`. Boot failed → `/admin/alerts/test-slack` + Better Stack Live tail unreachable for ~10 min until `cee7dfb` landed.

|                                | What                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`fix(deps)`**                | `pnpm install` regenerated lockfile in sync with package.json                                                                                                                                                                                                                                                                                                    |
| **`ci(husky)` (NEW gate 2/3)** | `pre-push` runs `pnpm install --frozen-lockfile --lockfile-only` BEFORE typecheck. Reads all workspace `package.json` files + verifies the lockfile satisfies them. Fast (~2s on warm cache). Returns non-zero with explicit remediation if drift detected. Renumbered: 1/3 typecheck (unchanged), **2/3 frozen-lockfile (NEW)**, 3/3 CHANGELOG guard (was 2/2). |

This catches the exact failure mode Day-5 hit. Future deps edits cannot ship without the lockfile in the same push range.

### Block C — BE: PR #20 opened — vector dim migration (M0's last code task)

`feature/be-m0-vector-384-migration` branch created with single commit `4954abb`:

| File                                                           | Change                                                                                                                                                                                                                  |
| -------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/api/prisma/raw/migrations/0002_vector_384_dim.sql` (NEW) | Canonical DROP-HNSW → ALTER-COLUMN-USING-NULL → RECREATE-HNSW inside single `BEGIN/COMMIT` transaction. Idempotent re-run via `IF EXISTS`. Tables empty at M0 (no real embeddings written yet) so `USING NULL` is safe. |
| `apps/api/prisma/schema.prisma`                                | `TestCase.embedding` + `KbChunk.embedding` flipped from `Unsupported("vector(1024)")?` → `Unsupported("vector(384)")?`. Doc comments now point at ADR-003 amendment + ADR-009 + this migration.                         |
| `apps/api/package.json`                                        | New `prisma:apply-raw:0002` script.                                                                                                                                                                                     |

**Visual gates:** typecheck exit 0; jest 84/84 PASS. **PR #20 opened, awaiting Yogesh's review + merge.** (Merged Day 6 morning at `28b009a` with rebased CHANGELOG conflict resolution.)

### Block D — BE: M1 prep on feature branch (`6bf56ca`, no PR until M0 closes)

`feature/be-m1-users-schema` branch — un-routed preview of M1 Users & Roles backend (TB-005 invitations + TB-004 project-scoped RBAC).

| Layer               | Files                                                                                                                                                                                                                                                                                                                                                 |
| ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Shared Zod          | `packages/shared/src/schemas/user.ts` extended: `AcceptInvitationInput` / `AcceptInvitationResponse` / `InvitationListItem` / `RevokeInvitationInput`. `projectScopeJson` typed as `z.array(Uuid)` (was `z.array(z.unknown())`).                                                                                                                      |
| Service             | `apps/api/src/invitations/invitations.service.ts` — `create` / `list` / `accept` / `revoke`, all audit synchronously. Token plaintext NEVER appears in audit payloads (security guarantee asserted).                                                                                                                                                  |
| Controller          | `apps/api/src/invitations/invitations.controller.ts` — POST/GET/POST-accept/DELETE under `/api/invitations`, RBAC matrix per PM1_PRD §3.2.                                                                                                                                                                                                            |
| Module              | `apps/api/src/invitations/invitations.module.ts` — **NOT registered in AppModule.imports**; M1 final PR adds the one-line import.                                                                                                                                                                                                                     |
| Project-scoped RBAC | `apps/api/src/auth/rbac/project-roles.{decorator,guard}.ts` — effective role = `TB-004.roleOverride ?? TB-002.role`, denials audit `rbac_project_denied`, falls back to workspace role when no project param. Cross-workspace IDs always 404 (never leak existence). Override `Stakeholder` demotes Admin in a single project (least-privilege wins). |

**Schema delta: ZERO.** TB-005 + TB-004 already provisioned in M0 — Track 2 reduced to services + guards + tests only. **107/107 jest PASS** (84 baseline + 23 new). Push used `--no-verify` per Yogesh's documented exception ("CHANGELOG only updates when main moves"). PR opens Sunday after M0 closes.

### Block E — FE: PR #21 opened — M1 prep (3 commits, DO NOT MERGE flag)

`feature/fe-m1-users-roles` branch:

| SHA       | Frame                                                                                                                                                            |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `f69c2b9` | F27 Users & Roles ported (Pattern A deferred markers, ADR-006 hooks, RWD: 320/768/1024/1440 viewport tested, AdminGuard route protection, hash deeplink for tab) |
| `3a8f865` | F27m1 Invite User Modal ported (Pattern A deferred — invite flow not wired in M1 prep; field shape locked)                                                       |
| `f106dbb` | F28 Settings & Audit ported + F27m1 exhaustive-deps fix (initial F27m1 had useEffect deps lint warning; carryover fix bundled here)                              |

PR #21 title: `feat(web): M1 prep — F27 + F27m1 + F28 admin frames (Pattern A, ADR-006, AdminGuard) [DO NOT MERGE — M0 OPEN]`. Sits on feature branch until M0 closes Sunday evening — then merges to main with Track 2 BE PR.

### Block F — PM/Cowork: T032 golden-set seeds parsed (R3 mitigation prep)

Spec lives in `Milestone_M0_Setup_v8.md` § T032 (R3 mitigation add-on). Source: real CPI Jira exports (114 Story+Task issues, 73 Bug issues) + Codex-augmented A2 dup pairs. Parser at `scripts/parse_golden_sets.py` (one-off, lives outside repo for now).

| Set         | Items                                                                                                          | Status                                           |
| ----------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| A1 Scribe   | 30 real CPI requirements (priority spread Highest 2 / High (migrated) 2 / Medium 9 / Minor 17 — top 30 of 114) | ✅ Ready (committed Day 6 at `75630f3`)          |
| A2 Sentinel | 54 easy + 48 hard = 102 duplicate pairs (Codex-generated alternative phrasings of REAL CPI bugs)               | ✅ Ready (committed Day 6 at `75630f3`)          |
| A4 Sherlock | 50 CPI defects (top 50 of 73 by signal richness — RCA + corrective actions + comments + steps)                 | ⚠️ Awaits L1-L5 tagging via Codex (Day 6 action) |

`apps/api/test/golden-sets/REVIEW_GUIDE.md` created (197 lines): explains 3 sets, L1-L5 taxonomy + decision tree, source provenance, eval-time methodology.

### Block G — Better Stack source provisioned (Path A, AC014 STRUCTURAL PASS)

Yogesh wired Render Log Stream → Better Stack qa-nexus-api source. **Configuration mistake**: I gave the wrong port (`:443` instead of `:6514`) — discovered Day 6 morning when zero logs arrived. Day-5 evening status: STRUCTURAL PASS (rules and pipeline configured, end-to-end fire pending verification). Real fire achieved Day 6 morning via OTel direct path (Render Free tier doesn't forward stdout to external destinations on free tier — only Organization+ — so we bypassed via NestJS OTel SDK → OTLP/HTTPS direct to Better Stack).

### Block H — Followup ledger updates

| ID      | Status before | Status after              | Notes                                                                                                                                                                                                                                                                                                                                                 |
| ------- | ------------- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **(n)** | —             | **FILED** (Day-5 evening) | OTel metrics SDK setup (MeterProvider + 3 named meters: embedding latency histogram + audit-log writes counter + LLM tokens counter labeled by provider/model). Day-6 / M0-close-window work. Owner: MAIN.                                                                                                                                            |
| **(o)** | —             | **FILED** (Day-5 evening) | FE/MAIN long-session image-dimension API errors (recurring twice during F28 commit). 3-layer permanent fix: (1) `.husky/pre-commit` `sips -Z 1500` auto-resize; (2) CLAUDE.md Hard Rule 14 (session restart per PR or 5 visual gates); (3) compact instruction guard against full locked-HTML inline loads. Owner: MAIN. (Landed Day 6 at `e62f122`.) |

---

## In flight

- **MAIN — followup (n) OTel metrics SDK:** MeterProvider + 3 meters (embedding latency histogram + audit-log writes counter + LLM tokens counter labeled by provider/model). Day 6 / M0-close-window scope.
- **MAIN — followup (o) image-dim fix:** 3-layer plan filed; implementation Day 6 morning. (Landed at `e62f122`.)
- **MAIN — Day-5 #5/#6/#7:** Skill audit refresh / code audit (last 9 commits since `b2a6235`) / AC gate dry-run prep for Sunday close. Day-6 work.
- **PM/Cowork — Day-5 EOD report:** This file. Owed per kickoff §5; deferred to Day 6 morning due to context window pressure on Day-5 evening.
- **Yogesh — A4 L1-L5 tagging via Codex:** 50 defects need root_cause_layer fills. Codex-driven, ~30 min review afterward. Day-6 action.

---

## Blockers

**None for M0.** Every track converged or has a known unblock path:

- **PR #20 (vector dim migration)** — opened Day 5; merge waits for Yogesh's eyeballs Day 6 morning. (Done Day 6 at `28b009a` with rebased CHANGELOG conflict resolution.)
- **AC014 end-to-end fire** — Day 5 STRUCTURAL PASS, real fire pending Day 6 (achieved via OTel direct path).
- **PR #21 (FE M1 prep)** — `[DO NOT MERGE — M0 OPEN]` flag is intentional; sits until Sunday.
- **BE Track 2 branch** — no PR until Sunday (Yogesh's explicit instruction; CHANGELOG-guard exception applies on feature branch).

**Watch items:**

- **Render Free tier log forwarding restriction** — discovered Day 6 morning during AC014 troubleshooting. Render Log Streams require Organization tier+ to forward stdout to external destinations. Bypassed via NestJS OTel SDK direct OTLP path. Documented in followup (post-M0) for permanent runbook capture.
- **Image-dimension API errors** — hit FE chat twice during F28 commit phase Day 5. Mitigated by session restart; permanent fix landed Day 6 at `e62f122`.

---

## Tomorrow (Day 6 — 2026-05-02, Saturday)

Top priorities for the morning + evening windows:

1. **Yogesh — merge PR #20 + run `prisma:apply-raw:0002`** (15 min, blocks AC020 final stamp)
2. **Yogesh — AC014 verification curl + alert E2E** (5 min, AC014 final stamp)
3. **Yogesh + Codex — A4 L1-L5 tagging** (30 min review, blocks T032 commit)
4. **MAIN — followup (o) implementation** (~30 min, image-dim 3-layer fix)
5. **MAIN — Day-5 #5/#6/#7 cleanup** (skill audit, code audit, AC gate dry-run prep)
6. **PM/Cowork — Day-5 EOD draft** (this file) + Day-6 EOD evening

Sunday 2026-05-03 = ceremonial M0 close: AC012 cron auto-fires + merge PR #21 + open BE Track 2 PR + write `docs/milestone-reports/M0-completion-2026-05-03.md`.

---

## Free-tier quota usage

- Cloudflare Pages: ~22 deploys / 500 mo
- GitHub Actions: ~78 min / 2,000 mo
- Neon: 0.0089 GB (9.16 MB) / 0.5 GB cap = **1.79%**
- Render: ~6 hrs runtime / free dyno
- Resend: 0 emails sent (M1 invite-flow ships in M1)
- R2: <0.01 GB transfer / 10 GB mo (only stub-uploads for F12 testing)
- Groq: 0 RPD / 1,000 RPD (`gpt-oss-120b` primary — gateway in DEFERRED mode, no real LLM calls yet)
- Gemini: 0 RPD / 1,500 RPD (fallback)
- **Total infra cost: $0/month confirmed.** ✓

---

## Cross-references

- `docs/CHANGELOG.md` — Day 5 morning [Unreleased] entries (a) PR #20 / (b) OTel SDK / (c) Better Stack Slack pipeline / (d) lockfile fix + spec amendments
- `docs/architecture/adr-003-embedding-model.md` — Day-4 amendment referenced by PR #20 + AC008 spec amendment
- `docs/architecture/adr-009-pnpm-sharp-render-deploy.md` — Day-4 ADR referenced by `cee7dfb` lockfile fix
- `docs/deploy/better-stack-runbook.md` — §7 added Day 5 (3 named alert rules + post-pilot tuning)
- `docs/SECURITY.md` — rotation table updated Day 5 (Grafana Cloud + Better Stack OTLP + Slack webhook rows)
- `apps/api/test/golden-sets/REVIEW_GUIDE.md` — created Day 5, finalized Day 6 (acceptance closure section)
- `docs/followups.md` — (n) OTel metrics SDK + (o) image-dim 3-layer fix filed
- `Milestone_M0_Setup_v8.md` — § T032 (golden-set seeds, M0 R3 mitigation) + § MS0-T017 + § AC008 amendments
