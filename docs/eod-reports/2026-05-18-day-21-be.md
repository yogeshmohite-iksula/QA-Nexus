# EOD — Day 21 (2026-05-18) — BE+1

> **Chat:** BE+1
> **Branch:** `docs/be-day-21-eod` (this PR)
> **Tag:** `[HOLD for M5 close docs wave]`
> **M5 timeline:** Sat May 23 close + Sun reserve · on track (Day 4 of 7)

---

## Completed today

- **`feat(api): sherlock orchestrator hardening — db + async/ws + audit [m5 da]`** — PR [#178](https://github.com/yogeshmohite-iksula/QA-Nexus/pull/178) (rebased post-cascade, `acd1d49`, awaiting merge). Closes the 3 hardening gaps from PR #173 synchronous path:
  - **(a) 5-layer RcaReport schema adapter** — new `SherlockOrchestratorService.runAndPersist(input, ctx)` method. AgentRun row (kind=A4) → 4-agent fan-out → RcaReport with 5-layer JSONB mapping (flake→L1, env→L2, L3 empty for future env-config split, code→L4, data→L5) → AgentRun complete → audit row → WS emit. Refactored fan-out into private `runFanout()` so sync `runRca()` (Day-20 preserved) + async `runAndPersist()` share logic.
  - **(b) Async 202+WS pattern** — `DefectsController.kickoffRca` flipped: resolves workspaceId via defect→project, pre-allocates runId, spawns orchestrator via `setImmediate`, returns `202 + { defectId, runId, accepted, wsChannel }`. `RealtimeGateway.emitRcaComplete(runId, payload)` — new public emit on existing channel-fanout pattern.
  - **(c) Audit writes per ERD §3.13 HMAC chain** — `rca_kicked_off` SYNC before 202 (a failed audit fails the request per `.claude/rules/api.md`). `rca_completed` / `rca_failed` SYNC within orchestrator scope, before WS emit. Failure path marks AgentRun failed + writes `rca_failed`, does NOT emit WS.
  - 49/50 suites passed at PR-open (1 pre-existing test-runs ESM trap fixed by PR #174). 12 new defects.controller tests for async contract. New `jest.mock` pattern for orchestrator.service spec (transitive better-auth ESM workaround — same Day-17 #138/#139 + #174 precedent).

- **`fix(api): kimi-k2 high triage — 4 items [m5]`** — PR [#181](https://github.com/yogeshmohite-iksula/QA-Nexus/pull/181) **MERGED 15:00 IST** (`d7681b5`). All 4 HIGH-severity items from Kimi K2's Day-19 security review closed:
  - **(c) HMAC secret loaded at boot, not per-write** — `AuditService.onModuleInit()` loads + validates `BETTER_AUTH_SECRET` once (≥32 chars, app crashes if missing — chain integrity binding per PM1_ERD §3.13 + Hard Rule 7). Stored in `private readonly this.secret`. `writeAuditRow()` now takes secret as required param. Previously read `process.env` on every write + every `verifyChain()` call — slow + late-failing + could silently sign half the chain with old secret + half with new during rotation.
  - **(d) Auth Zod schemas → `@qa-nexus/shared`** — new `packages/shared/src/schemas/auth.ts` exports `SignUpBodySchema` + `SignInBodySchema` + inferred types. FE can now use same schema for react-hook-form + zodResolver, preventing drift.
  - **(a) callbackURL trusted-origins allowlist** — closes magic-link → phishing chain (`?callbackURL=https://evil.com`). New `apps/api/src/auth/callback-url.ts` with `parseTrustedOrigins()` + `isTrustedCallbackUrl()` (+14 unit tests). Defense-in-depth: sign-up/sign-in intake rejects 400; callback redirect rewrites to `/home`. `TRUSTED_CALLBACK_ORIGINS` env CSV; same-origin paths (`/home`) always allowed; protocol-relative (`//evil.com`) always rejected.
  - **(b) WS rate limit + connection ceiling** — `RealtimeGateway.handleConnection` adds two layers BEFORE Prisma auth lookup (cheap reject before CU-hr spend): global ceiling via `MAX_WS_CONNECTIONS` env (default 100, close 4409); per-IP token bucket (capacity 10, refill 10/min, close 4408). Per-IP buckets GC'd every 60s after 5min idle. `connectionCount` decremented in `handleDisconnect` with double-disconnect clamp. 8-user pilot has 12.5x ceiling headroom + 60x bucket headroom — guards catch abuse, never normal traffic.
  - 51/51 suites pass; 607 tests pass (+14 callback-url tests; 5 verifyChain tests reworked for boot pattern).

- **Rebase wave** — rebased PR #165 (BE Day-19 EOD, `3d704b1 → e1354f7`) + PR #175 (BE Day-20 EOD, `636be4f → 06b714a`) on fresh main pre-cascade. Both clean (no CHANGELOG conflict markers despite the original flag). One hiccup on #175 (prettier on untracked `sessions/<handoff>.md` — fixed via `prettier --write` on that file only). #175 #178 (post-cascade rebase via `git rebase --skip` to drop duplicate #177 commit `9cf85dd`).

## In flight

- **PR #178 (Sherlock hardening da)** — REBASED + force-pushed (`acd1d49`); `mergeable=MERGEABLE`; CI re-running on rebased SHA (`UNSTABLE` until checks finish, pre-rebase was all green). Owned by: Yogesh to merge.
- **This PR (Day-21 BE EOD)** — `docs/be-day-21-eod` with this file; HOLD for M5 close docs wave.

## Blockers

- **None** at EOD. All blockers resolved earlier today:
  - **AM blocker (test-runs ESM trap):** PR #174 merged early afternoon; unblocked #177 + #178 + future BE PRs from same test gate.
  - **Branch lineage hiccup on Kimi work:** parallel session accidentally committed `docs(arch): adr-020 jira sync architecture` (`be1b061`) onto the original `fix/api-kimi-k2-high-triage-m5` branch. That commit is properly covered by PR [#179](https://github.com/yogeshmohite-iksula/QA-Nexus/pull/179). Recovered cleanly by cherry-picking my 5 Kimi commits onto fresh main as `fix/api-kimi-k2-high-triage-m5-v2`; the original polluted branch can be deleted from remote (no PR attached). Lessons-learned candidate for the M5 retro.

## Tomorrow (Day 22, Tue 2026-05-19)

1. **P0 — xlsx + pdf-parse CVE replacements** (~2-4 hr) — deferred from Day-21 P3. Plan: identify currently-pinned versions in `apps/api/package.json`; check advisory database for safe minimums; pin or replace. Bundle with `(da)` followup PR if `xlsx` consumer touches the orchestrator path, or stand-alone otherwise.
2. **P1 — Jira sync wire-up prep against ADR-020** (~2-3 hr) — ADR-020 landed today as [#179](https://github.com/yogeshmohite-iksula/QA-Nexus/pull/179) (`docs/main-adr-020-jira-sync`). Read the ADR fully + draft skeleton for `JiraSyncService` (webhook ingestion handler + REST backfill loop + QNT seed wiring). Implementation lands Wed Day-23.
3. **P2 — Day-22 EOD report** — same cadence (17:00-18:00 IST).
4. **Standing watch** — PR #178 if it doesn't merge today; any AM ask from Yogesh on M5 close path.

## Free-tier quota usage

- Cloudflare Pages: ~0 deploys today (BE-only PRs) / 500 mo
- GitHub Actions: ~25 min today (4 PRs × CI runs ~5 min avg) / 2,000 mo
- Neon: ~82/100 CU-hr (flat; Sherlock hardening adds ~+0.5-1 CU-hr/day at pilot load — safely under cap)
- Render: ~12 hr / free dyno (pilot operating window 10AM-10PM)
- Resend: 0 emails today / 3,000 mo
- R2: 0 GB transfer today / 10 GB mo
- Groq: 0 req today (no LLM calls; Sherlock RCA hardening did not trigger real fan-outs) / 1,000 RPD (gpt-oss-120b primary)
- Gemini: 0 req today / 1,500 RPD (fallback)
- **Total infra cost: $0/month confirmed.** ✓

## Notes for the M5 retro

Three small lessons-learned to capture next week:

1. **Worktree confusion across parallel chats.** Multiple session checkouts during a single day produced a branch where my Kimi work landed on `docs/main-day-21-eod` (wrong branch), and an unrelated `adr-020` commit landed on `fix/api-kimi-k2-high-triage-m5` (also wrong branch). Recovered cleanly via cherry-pick onto a fresh `-v2` branch. Consider: explicit `git branch --show-current` check before every commit batch on long-running sessions.

2. **Rebase + duplicate-commit detection.** PR #178 had `9cf85dd feat(claude): skill v2.2` as a parent commit — same content as PR #177's `098ebd6` already on main. `git rebase` correctly flagged it as conflict; `git rebase --skip` resolved cleanly. Worth adding to runbook: "rebase conflict on a commit whose content is already on main → skip, don't manually resolve."

3. **Pre-push CHANGELOG guard works.** Saved the Kimi PR from shipping without a CHANGELOG entry. The "bump for kimi-k2 high triage" was authored ONLY because the guard nudged. Good.

---

## Cross-references

- [PR #178](https://github.com/yogeshmohite-iksula/QA-Nexus/pull/178) — Sherlock hardening (da) — awaiting merge
- [PR #181](https://github.com/yogeshmohite-iksula/QA-Nexus/pull/181) — Kimi K2 HIGH triage — MERGED 15:00 IST
- [PR #165](https://github.com/yogeshmohite-iksula/QA-Nexus/pull/165) — BE Day-19 EOD — rebased
- [PR #175](https://github.com/yogeshmohite-iksula/QA-Nexus/pull/175) — BE Day-20 EOD — rebased
- ADR-019 — Sherlock 5-layer architecture (informed (da))
- PM1_ERD §3.13 — HMAC-chained audit log (binding for both (da) audit writes + Kimi-c HMAC-at-boot)
- `.claude/rules/api.md` — audit must be sync within request handler
- CLAUDE.md Hard Rule 7 — audit chain binding

🤖 Generated with [Claude Code](https://claude.com/claude-code)
