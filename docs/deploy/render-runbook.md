# Render API deploy runbook (T011)

**Owner:** Yogesh (dashboard work) + MAIN (post-deploy wiring).
**Target:** NestJS API live at `https://qa-nexus-api.onrender.com`, `GET /health` returns 200 with all subsystem readouts.
**Cost:** $0/month (Render Free Hobby plan — single dyno, 512 MB RAM, sleeps after 15 min idle).
**Closes:** MS0-T011, MS0-AC002.

---

## Prerequisites — verify before starting

- [ ] Render account exists. Email = `<your Iksula corporate email>`. If not, sign up at https://render.com (free, no credit card).
- [ ] You have GitHub admin access on `yogeshmohite-iksula/QA-Nexus` (needed to grant Render webhook permission).
- [ ] Neon DB is provisioned (T012). You have `DATABASE_URL` and `DIRECT_URL` strings ready.
- [ ] You have the **`BETTER_AUTH_SECRET`** value generated by MAIN (see "Env vars" table below — the value is at the bottom of this runbook to paste into Render).
- [ ] You have time for cold-start (first deploy ~5 min build + ~38s embedding model download = ~6 min total).

---

## Step 1 — Account setup verification

1. Sign in to https://render.com.
2. Navigate to **Account Settings → Plan**.
3. Confirm: **Plan = Free** (no credit card on file). If you see "Hobby ($7/mo)", that's the OLD plan — Render renamed it. The Free tier is still 750 dyno-hours/month.
4. Free-tier limits to know:
   - **750 hours/month** of single-dyno runtime (more than enough for a single 24/7 service).
   - **512 MB RAM** per dyno.
   - **Sleeps after 15 min idle** (we mitigate via UptimeRobot 5-min ping — see `uptimerobot-runbook.md`).
   - **No outbound IP whitelisting needed** (Neon allows all IPs by default).

---

## Step 2 — Create the Web Service

1. From Render dashboard, click **New + → Web Service**.
2. Connect GitHub: select **yogeshmohite-iksula/QA-Nexus** repo.
   - If "Render needs permission" → grant access to the single repo (least-privilege).
3. Fill in service config (use these EXACT values):

| Field          | Value                                                                                       |
| -------------- | ------------------------------------------------------------------------------------------- |
| Name           | `qa-nexus-api`                                                                              |
| Region         | **Singapore** (matches Neon DB region for low-latency queries)                              |
| Branch         | `main`                                                                                      |
| Root Directory | (leave blank — monorepo root works)                                                         |
| Runtime        | **Node** (auto-detected from package.json)                                                  |
| Build Command  | `pnpm install && pnpm --filter @qa-nexus/shared build && pnpm --filter @qa-nexus/api build` |
| Start Command  | `pnpm --filter @qa-nexus/api start:prod`                                                    |
| Plan           | **Free** ($0/month)                                                                         |
| Auto-Deploy    | **Yes** (deploy on push to `main`)                                                          |

4. **Don't click "Create Web Service" yet** — we need to add env vars first (Step 3). If you accidentally create early, the first deploy will fail; just add env vars then click "Manual Deploy → Clear build cache & deploy".

---

## Step 3 — Environment variables matrix

In the Render service settings, click **Environment** tab. Add each row:

| Name                                | Value (paste)                               | Source / Notes                                                                                                                                                                                                                        | Required for first deploy?       |
| ----------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| `NODE_ENV`                          | `production`                                | —                                                                                                                                                                                                                                     | ✅ yes                           |
| `PORT`                              | `3001`                                      | Render auto-injects `$PORT` — this is a fallback. NestJS `main.ts` uses `process.env.PORT \|\| 3001`.                                                                                                                                 | ✅ yes                           |
| `DATABASE_URL`                      | (from Neon dashboard — pooled URL)          | Pooled connection (port 6543). Used at runtime.                                                                                                                                                                                       | ✅ yes                           |
| `DIRECT_URL`                        | (from Neon dashboard — direct URL)          | Non-pooled (port 5432). Used by `prisma migrate deploy` only. For PM1 pilot, can = DATABASE_URL.                                                                                                                                      | ✅ yes                           |
| `BETTER_AUTH_SECRET`                | (see bottom of this runbook)                | 32-byte base64 random. NEVER reuse across environments.                                                                                                                                                                               | ✅ yes                           |
| `BETTER_AUTH_URL`                   | (set AFTER first deploy)                    | Will be `https://qa-nexus-api.onrender.com` — paste this AFTER first deploy succeeds + Render assigns the URL. **First deploy WILL log a BetterAuth warning** about missing URL — that's expected and self-resolves on second deploy. | ⚠️ no (paste after first deploy) |
| `EMBEDDING_MODEL_ID`                | `Xenova/bge-large-en-v1.5`                  | Per ADR-003.                                                                                                                                                                                                                          | ✅ yes                           |
| `RESEND_API_KEY`                    | (deferred — set after T014)                 | Until set, magic-link emails log to console (stub mode). Real email needs this.                                                                                                                                                       | ⚠️ no                            |
| `R2_ACCESS_KEY_ID`                  | (deferred — set after T013)                 | Until set, /storage/\* endpoints return 503 + /health reports r2.status="deferred".                                                                                                                                                   | ⚠️ no                            |
| `R2_SECRET_ACCESS_KEY`              | (deferred — set after T013)                 | "                                                                                                                                                                                                                                     | ⚠️ no                            |
| `R2_BUCKET`                         | `qa-nexus-evidence-pm1`                     | Bucket name (set even before R2 token exists).                                                                                                                                                                                        | ✅ yes (cosmetic)                |
| `R2_ENDPOINT`                       | (deferred — set after T013)                 | `https://<account-id>.r2.cloudflarestorage.com`                                                                                                                                                                                       | ⚠️ no                            |
| `LLM_PRIMARY_PROVIDER`              | `groq`                                      | Default per CLAUDE.md.                                                                                                                                                                                                                | ⚠️ no                            |
| `LLM_PRIMARY_MODEL`                 | `openai/gpt-oss-120b`                       | Per CLAUDE.md "LLM primary".                                                                                                                                                                                                          | ⚠️ no                            |
| `LLM_PRIMARY_API_KEY`               | (deferred — set after T023)                 | From https://console.groq.com/keys.                                                                                                                                                                                                   | ⚠️ no                            |
| `LLM_SECONDARY_PROVIDER`            | `gemini`                                    | Per CLAUDE.md fallback.                                                                                                                                                                                                               | ⚠️ no                            |
| `LLM_SECONDARY_MODEL`               | `gemini-2.5-flash`                          | "                                                                                                                                                                                                                                     | ⚠️ no                            |
| `LLM_SECONDARY_API_KEY`             | (deferred — set after T023)                 | From https://aistudio.google.com/apikey.                                                                                                                                                                                              | ⚠️ no                            |
| `LLM_LONG_CONTEXT_PROVIDER`         | `groq`                                      | Optional overflow chain.                                                                                                                                                                                                              | ⚠️ no                            |
| `LLM_LONG_CONTEXT_MODEL`            | `meta-llama/llama-4-scout-17b-16e-instruct` | "                                                                                                                                                                                                                                     | ⚠️ no                            |
| `LLM_LONG_CONTEXT_THRESHOLD_TOKENS` | `100000`                                    | Switch to long-context model when prompt > 100k tokens.                                                                                                                                                                               | ⚠️ no                            |
| `GRAFANA_CLOUD_OTLP_ENDPOINT`       | (after T019 lands)                          | OTLP HTTP endpoint from Grafana Cloud's Connections → OpenTelemetry source. Receives traces + metrics. Service runs in deferred-mode if unset.                                                                                        | ⚠️ no (T019)                     |
| `GRAFANA_CLOUD_OTLP_AUTH`           | (after T019 lands)                          | `base64(grafana_user:grafana_api_token)` — set as Basic auth header. Generated when you create the Grafana Cloud OpenTelemetry source.                                                                                                | ⚠️ no (T019)                     |
| `BETTER_STACK_OTLP_ENDPOINT`        | (after T019 lands)                          | OTLP HTTP endpoint from Better Stack's "OpenTelemetry" source (NOT the Pino or Generic source — see `better-stack-runbook.md`). Receives logs.                                                                                        | ⚠️ no (T019)                     |
| `BETTER_STACK_OTLP_AUTH`            | (after T019 lands)                          | Source token from Better Stack's Connect panel — set as Bearer auth header.                                                                                                                                                           | ⚠️ no (T019)                     |

**Tip:** "Required for first deploy = ✅ yes" means the service will START without it but will log a runtime warning. The four ✅-yes items are the bare minimum to get past `nest start` boot.

**T019 deferred-mode behavior:** with all four `GRAFANA_CLOUD_*` and `BETTER_STACK_*` env vars unset, the service starts cleanly, logs go to stdout (visible in Render's web console + via `render logs`), and `/health` reports `otel.{traces,logs}.exporter = "deferred"`. Set the env vars + redeploy to flip to live OTLP export. Both Grafana Cloud (traces + metrics) and Better Stack (logs) can be wired independently — wiring just one is fine.

---

## Step 4 — First deploy (and the gotchas)

Click **Create Web Service**. Render will:

1. Clone the repo at `main` HEAD.
2. Run the build command (`pnpm install && pnpm --filter @qa-nexus/shared build && pnpm --filter @qa-nexus/api build`). Expect ~3-4 min on first deploy (cold pnpm cache).
3. Run the start command. NestJS bootstraps + binds to `$PORT`.
4. **Important: the embedding model (`@xenova/transformers` + `Xenova/bge-large-en-v1.5`) loads on bootstrap.** First boot downloads ~660 MB of ONNX assets from Hugging Face → takes ~38s. Subsequent boots use the local FS cache (~1.7s). Render's free dyno has 512 MB RAM, but the model fits because it's WASM (~280 MB resident at warm).

### First-deploy gotchas

**Gotcha 1: Prisma migration not auto-run.** Render's build command does NOT run `prisma migrate deploy`. The schema must already exist on Neon. **Fix:** before first deploy, run from your local:

```bash
DATABASE_URL=<neon-pooled-url> DIRECT_URL=<neon-direct-url> pnpm db:migrate:deploy
DATABASE_URL=<neon-pooled-url> DIRECT_URL=<neon-direct-url> pnpm db:apply-raw
```

Both schemas (Prisma migrations + raw RLS/HNSW) need to land before the API can serve auth requests.

**Gotcha 2: BETTER_AUTH_URL chicken-and-egg.** Render assigns the URL AFTER first deploy succeeds. So `BETTER_AUTH_URL` is unset on first deploy → BetterAuth logs a warning + falls back to `http://localhost:3001`. Magic-link emails will reference localhost. **Fix:** after first deploy succeeds (any HTTP response from /health), copy the assigned URL (e.g. `https://qa-nexus-api.onrender.com`) into the env-var panel, save, click "Manual Deploy → Deploy latest commit". Second deploy uses the right URL.

**Gotcha 3: Embedding model OOM on free dyno.** 512 MB RAM is tight when (a) NestJS + Prisma + (b) the bge-large model are both warm. If you see `WORKER OOM` in logs:

- Switch `EMBEDDING_MODEL_ID` to a smaller model temporarily: `Xenova/bge-small-en-v1.5` (384-dim, ~100 MB resident). **Note:** this changes vector dim — schema migration required (`vector(1024)` → `vector(384)`). Don't do this lightly. Better path: upgrade to a paid Render dyno temporarily during pilot scale-up.
- OR: lazy-load the model on first /embedding request instead of eager-load at bootstrap. Code change: `apps/api/src/embedding/embedding.service.ts` — defer `pipeline()` call to first `embed()` call. Costs ~38s latency on the first request post-deploy but keeps boot under 5s.

**Gotcha 4: Cold-start latency.** Render free dyno sleeps after 15 min of no requests. Cold-start = ~10-15s (including model warm-up if cache survives, ~40s if cache evicted). UptimeRobot pings /health every 5 min during operating window (10 AM – 10 PM IST) to keep the dyno warm. See `uptimerobot-runbook.md`.

---

## Step 5 — Post-deploy verification

Once Render reports "Live" status, run these from your local terminal (replace URL with the actual assigned one):

```bash
# 1. Basic reachability
curl -s -o /dev/null -w "HTTP %{http_code}\n" https://qa-nexus-api.onrender.com/health
# Expected: HTTP 200

# 2. Full health readout
curl -s https://qa-nexus-api.onrender.com/health | jq
# Expected fields:
#   status: "ok"
#   db: { up: true, latency_ms: <number> }
#   embedding: { warm: true, model: "Xenova/bge-large-en-v1.5", dim: 1024 }
#   r2: { status: "deferred", reason: "missing credentials" }    ← until T013
#   llm: { status: "deferred", reason: "missing API keys" }      ← until T023
#   quota: { db_size_mb: 9.4, db_size_pct: 1.83 }

# 3. Public auth endpoints reachable
curl -s -o /dev/null -w "sign-up:  HTTP %{http_code}\n" https://qa-nexus-api.onrender.com/auth/sign-up
curl -s -o /dev/null -w "sign-in:  HTTP %{http_code}\n" https://qa-nexus-api.onrender.com/auth/sign-in
# Expected: both 400 (missing body) — proves the routes exist and Nest is up

# 4. RBAC blocks anon to protected endpoint
curl -s -o /dev/null -w "rbac-demo:  HTTP %{http_code}\n" https://qa-nexus-api.onrender.com/rbac-demo/admin-only
# Expected: 401 (not authenticated)
```

If all four pass, **paste the URL back into this chat** and MAIN will:

1. Update `apps/api/.env.example` with the canonical URL.
2. Update `docs/STATUS.md` Last Deploy + Health Check rows.
3. Update `BETTER_AUTH_URL` in Render env vars (you can paste it once you've shared the URL).
4. Trigger a re-deploy so BetterAuth picks up the URL.

---

## Step 6 — Rollback procedure

Render keeps the last 5 successful deploys. To roll back:

1. Render dashboard → service → **Events** tab.
2. Find the last good deploy (look for green "Live" badge).
3. Click the deploy → **Rollback to this deploy**.
4. Render redeploys the previous Docker image. ~30s downtime.
5. **Important:** rollback does NOT undo database migrations. If a migration broke prod, you also need to run `prisma migrate resolve` from your local — see `docs/architecture/adr-002-prisma-raw-split.md` § "Re-apply semantics".

---

## Step 7 — Common errors + fixes

| Symptom                                                    | Likely cause                           | Fix                                                                                      |
| ---------------------------------------------------------- | -------------------------------------- | ---------------------------------------------------------------------------------------- |
| Build fails: `Module not found: @qa-nexus/shared`          | Build command didn't pre-build shared  | Verify build command exactly matches Step 2 table (chains shared build first)            |
| Boot fails: `Cannot find module '../shared/dist'`          | Same as above                          | "                                                                                        |
| Boot fails: `MODULE_NOT_FOUND: better-auth/node`           | esModuleInterop config issue           | Verify `apps/api/tsconfig.json` has `"esModuleInterop": true` (already set Day 2)        |
| Logs show `Falling back to localhost:3001` from BetterAuth | `BETTER_AUTH_URL` unset (first deploy) | Paste the Render URL into env vars + redeploy                                            |
| Logs show `WORKER OOM` after ~40s                          | Embedding model OOM on 512 MB          | See Gotcha 3 above                                                                       |
| `/health` returns 503 + db.up=false                        | Neon connection limit (100) hit        | Check Neon dashboard "Connections" — stale connections from local dev? Restart local API |
| Dyno restarts every ~10s                                   | Build healthcheck failing              | Render's deploy healthcheck = `GET /health`. Make sure it returns 200 on first call.     |

---

## Step 8 — Cost confirmation

After 24h of running:

- Render dashboard → **Billing** tab.
- Confirm: **$0.00 due**, **0 / 750 hours used** (or close to that).
- If hours > 24 (i.e., service used > 1 hour-of-month per hour-of-clock), something's wrong — possibly multiple workers spawning. Check `npx --yes ts-node --version` log line in build output to confirm only one process.

---

## ⚠️ SECRETS BELOW — for Yogesh to copy into Render env vars panel

**`BETTER_AUTH_SECRET`** (generated by MAIN via `openssl rand -base64 32` on 2026-04-29):

```
GbjqJTOBNkl3LRXj5nwH8NS5MaFwbGv/NXuPG5HgEmE=
```

**Important:**

- This value is in this runbook ONLY because the runbook is for a single human (Yogesh) to paste into Render's dashboard. It is NOT in any committed `.env` file.
- The `.gitleaks.toml` allowlist treats `docs/deploy/render-runbook.md` as a known-secret-OK file (added in this same change-set; verify before push).
- If this secret leaks, regenerate via `openssl rand -base64 32`, paste into Render, restart dyno → all existing sessions invalidate (BetterAuth re-signs cookies with new key).

---

## Cross-references

- `docs/architecture/adr-004-render-deployment.md` — the ADR documenting WHY Render Free over alternatives
- `docs/architecture/adr-011-m2-staging-deployment.md` — Day-11 amendment for M2 staging (see new section below)
- `docs/deploy/r2-runbook.md` — sister runbook for T013 R2 storage
- `docs/deploy/uptimerobot-runbook.md` — sister runbook for T015 keep-alive
- `docs/deploy/resend-runbook.md` — sister runbook for T014 magic-link email
- `apps/api/.env.example` — env-var reference (placeholders only)
- `apps/api/src/main.ts` — NestJS bootstrap (binds `$PORT`)
- `CLAUDE.md` § "Locked tech stack" — Render commitment
- `PM1_ERD §M0_v8` — task T011 spec

---

## Day-11 amendment — M2 staging deployment (`qa-nexus-api-staging`)

> Spec: ADR-011. Adds a SECOND Render service alongside `qa-nexus-api`
> (M0 production). The original setup above stays authoritative for
> the M0 service; everything below applies to the new staging service.

### Why a second service

M2 (Knowledge Base & RAG) ships features that need a real Postgres DB
exercising pgvector HNSW indexes — local SQLite stubs are no longer
sufficient. A dedicated staging environment lets us:

- Validate magic-link sign-in end-to-end against `app.qanexus.iksula.com` ↔ `api.qanexus.iksula.com` cross-subdomain cookies (ADR-007)
- Exercise the chunking → embedding → search → answer RAG pipeline against real pgvector(384) HNSW indexes
- Run M2 acceptance gates Sat 9 May with `verify:audit` against a populated `audit_log`
- Let FE+1 run Pattern B real BetterAuth wiring against a live API without local-DB friction

### Step 1 — Create the Neon staging Postgres project

1. Sign in to https://console.neon.tech.
2. Click **New Project**.
3. Name: `qa-nexus-staging`.
4. Region: `AWS us-east-2 (Ohio)` — closest to Render `oregon` (~80 ms RTT vs ~200 ms from Singapore).
5. Postgres version: `17` (latest stable).
6. Click **Create**.
7. Once provisioned, navigate to **SQL Editor** + run:
   ```sql
   CREATE EXTENSION IF NOT EXISTS vector;
   ```
   (Enables pgvector on the staging DB. The schema migrations also assert it's present, but creating it explicitly here means the first `prisma migrate deploy` doesn't fail.)
8. Navigate to **Connection Details** + copy two strings:
   - **Pooled connection string** — ends in `-pooler.us-east-2.aws.neon.tech`. **This is `DATABASE_URL`.** Append `?pgbouncer=true&connection_limit=1` to the URL if not already present (Prisma serverless requirement).
   - **Direct connection string** — same hostname WITHOUT `-pooler`. **This is `DIRECT_URL`.** Used only by `prisma migrate deploy`.

### Step 2 — Apply the Render Blueprint

1. Make sure `render.yaml` at repo root has a service named `qa-nexus-api-staging` (it does; landed in this PR).
2. In the Render dashboard, click **New +** → **Blueprint**.
3. Connect to `yogeshmohite-iksula/QA-Nexus` (the GitHub authorization from MS0-T011 should still be active).
4. Render auto-detects `render.yaml` + lists both services (`qa-nexus-api` + `qa-nexus-api-staging`). Make sure ONLY the staging service is checked for new creation; the M0 service is already running and shouldn't be redeployed via Blueprint.
5. Click **Apply**.
6. Render provisions `qa-nexus-api-staging`. The first build will fail (env vars not yet set) — that's expected, fill them in Step 3.

### Step 3 — Fill the `sync: false` env vars (~10 min)

In the Render dashboard for `qa-nexus-api-staging`:

| Var                           | Value                                                                | Source                 |
| ----------------------------- | -------------------------------------------------------------------- | ---------------------- |
| `DATABASE_URL`                | Neon pooled URL from Step 1.7                                        | Neon                   |
| `DIRECT_URL`                  | Neon direct URL from Step 1.7                                        | Neon                   |
| `BETTER_AUTH_SECRET`          | (leave empty — Render auto-generates per `generateValue: true`)      | Render                 |
| `BETTER_AUTH_COOKIE_DOMAIN`   | (leave UNSET initially; set to `.qanexus.iksula.com` after DNS swap) | —                      |
| `GROQ_API_KEY`                | `gsk_...` from https://console.groq.com/keys                         | Groq                   |
| `GEMINI_API_KEY`              | `AIza...` from https://aistudio.google.com/app/apikey                | Gemini                 |
| `RESEND_API_KEY`              | Resend API key — `re_...` from https://resend.com/api-keys           | Resend (ADR-018)       |
| `RESEND_FROM_EMAIL`           | Pilot: `onboarding@resend.dev` · Prod: `noreply@qanexus.iksula.com`  | Resend / followup (bg) |
| `RESEND_REPLY_TO`             | `yogesh.mohite@iksula.com`                                           | Iksula                 |
| `RESEND_BCC_EMAIL`            | `yogesh.mohite@iksula.com` (silent pilot-tracking copy)              | Iksula                 |
| `R2_ACCOUNT_ID`               | Cloudflare → R2 → API Tokens                                         | R2                     |
| `R2_ACCESS_KEY_ID`            | (token client ID)                                                    | R2                     |
| `R2_SECRET_ACCESS_KEY`        | (token client secret)                                                | R2                     |
| `R2_ENDPOINT`                 | `https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com`                   | derived                |
| `R2_PUBLIC_URL`               | `https://uploads.qa-nexus.iksula.com` OR `*.r2.dev` fallback         | R2                     |
| `OTEL_EXPORTER_OTLP_ENDPOINT` | Better Stack ingest URL                                              | Better Stack           |
| `OTEL_EXPORTER_OTLP_HEADERS`  | `Authorization=Bearer <token>`                                       | Better Stack           |

All other env vars are pre-filled from the Blueprint (see `render.yaml`).

### Step 4 — Manual redeploy

After filling env vars, click **Manual Deploy → Deploy latest commit** in the Render dashboard. Build should succeed this time:

- `pnpm install --frozen-lockfile` (~60 s)
- `pnpm rebuild sharp` (~10 s)
- `pnpm --filter @qa-nexus/shared build` (~5 s)
- `pnpm --filter @qa-nexus/api build` (~30 s)
- `pnpm --filter @qa-nexus/api prisma:migrate:deploy` (~15 s for first-time migration apply)
- Total build: ~2 min
- First boot: ~30 s (NestJS init + embedding model download)

### Step 5 — Smoke test post-deploy

```bash
./scripts/smoke-test-render.sh
# OR for a custom URL:
./scripts/smoke-test-render.sh https://qa-nexus-api-staging.onrender.com
```

Validates 7 things in ~5 s:

1. `/health` returns 200 + parseable JSON
2. `/health.db.status === up` (Neon connection live)
3. `/health.embedding.status` is `up` OR `deferred` (NOT `down`)
4. LLM gateway path responds with non-5xx (501 deferred OK pre-F26)
5. `POST /api/auth/sign-in/magic-link` is wired (non-5xx response)
6. `/api/users` returns 401/403 (RBAC guard active)
7. `/health` response time < 5 s (catches cold-start regressions)

Exits non-zero on any failure — grep "FAIL" in output to see which checks fell over.

### Step 6 — UptimeRobot keep-alive

Add a SECOND UptimeRobot monitor alongside the M0 one:

- **URL**: `https://qa-nexus-api-staging.onrender.com/health`
- **Interval**: 5 min
- **Window**: 04:30 – 16:30 UTC (= 10 AM – 10 PM IST)
- **Alert contact**: same Slack webhook as the M0 monitor

See `docs/deploy/uptimerobot-runbook.md` for the dashboard walk-through.

### Step 7 — DNS swap-in (deferred to M3)

Once `api.qanexus.iksula.com` DNS is configured (Cloudflare → DNS → CNAME → `qa-nexus-api-staging.onrender.com`), update the Render dashboard:

- **Custom Domains** → **Add Custom Domain** → `api.qanexus.iksula.com`
- Update env vars:
  - `BETTER_AUTH_URL` = `https://api.qanexus.iksula.com`
  - `BETTER_AUTH_COOKIE_DOMAIN` = `.qanexus.iksula.com` (per ADR-007)
- Manual redeploy

Cookie domain switch is non-trivial — verify cross-subdomain SSO works after the swap (`scripts/smoke-test-render.sh` doesn't catch this; needs a browser session test).

### Acceptance gate

Staging deploy is GREEN when:

- [ ] `scripts/smoke-test-render.sh` exits 0 (all 7 checks pass)
- [ ] First magic-link send to `yogesh.mohite@iksula.com` lands in inbox (NOT spam)
- [ ] First sign-in triggers Day-0 admin auto-promote (audit row `day0_admin_seeded` present)
- [ ] `pnpm --filter @qa-nexus/api verify:audit` passes against staging DB
- [ ] UptimeRobot reports 100% uptime over a 1-hour test window
