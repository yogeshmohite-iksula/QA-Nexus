# Hard Rule 17 self-audit — F19 + F20 + F21 canned-data vs v2 HTML

**Date:** 2026-05-14 (Day-18 evening)
**Method:** Exhaustive grep — every string literal in `canned-data.ts` checked against the source v2 HTML (HTML-entity normalised, whitespace-collapsed). Whitelist: CSS vars, design tokens, segment-kind discriminators (`'text' | 'bold'` etc.), pure-numeric values, enum keys.
**Branches audited:**

- F19 — `origin/main:apps/web/components/runs/canned-data.ts` (merged via PR #135 squash)
- F20 — `origin/feature/fe-m4-f20-canon-extract-fix:apps/web/components/results/canned-data.ts` (PR #150, holding)
- F21 — `origin/wip/f21-pre-tooling-fix:apps/web/components/defects/canned-data.ts` (parked, PR #152 closed)
  **Audit script:** `/tmp/rule17-audit.mjs`

## Summary

| Frame   | Strings scanned | Violations | Violation rate        |
| ------- | --------------- | ---------- | --------------------- |
| **F19** | 150             | 7          | 4.7 %                 |
| **F20** | 188             | **19**     | **10.1 %** ⚠️ highest |
| **F21** | 184             | 2          | 1.1 % ✅ cleanest     |

## F19 violations (7)

| #   | String                                         | Likely classification                                                                                                                                                                       |
| --- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `"production"`                                 | Enum value in JSDoc/comment (`env: 'staging' \| 'production' \| etc.`) — likely false positive                                                                                              |
| 2   | `"Refund webhook receives event"`              | Possible paraphrase of canonical L703 `"Refund webhook receives refund.retry.exhausted — handler timeout"` — need check                                                                     |
| 3   | `"screenshots"`                                | Likely evidence-type enum key (not user-visible)                                                                                                                                            |
| 4   | `"network"`                                    | Likely evidence-type enum key                                                                                                                                                               |
| 5   | `"dom"`                                        | Likely evidence-type enum key                                                                                                                                                               |
| 6   | `"plain"`                                      | Likely evidence-type enum key                                                                                                                                                               |
| 7   | `"matches 2 prior incidents from 2026-03-14."` | **Cross-frame leak** — this string is canonical for F20 cluster 1 (PR #150 canned-data L211) but my F19 canned-data references it (likely in a docstring/comment example, not user-visible) |

**F19 verdict:** ~4 of 7 are false positives (enum keys + JSDoc); 2-3 may be real but low-severity (Day-17 work, pre-Hard-Rule-17 codification).

## F20 violations (19) — primary concern

### Invented case row titles (8 violations)

Canonical HTML showed only 2-3 case rows per suite; I padded each suite to 3 rows by inventing titles + durations for the additional rows.

| Suite             | Invented case                                                                    | Status                                                                                                                                                                                                                   |
| ----------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Refund Core       | `"Refund webhook receives refund.retry.exhausted — handler timeout"` / `"11.1s"` | **Wait — title IS in F19 canonical L703 (selected case panel). My F20 reuses it. Canonical F20 has TC-RET-0342 as a case row but only shows the cr-title up to the truncate. Cross-frame reuse but not pure invention.** |
| Refund Core       | `"Refund exceeding order total — eligibility guard"` / `"10.8s"`                 | **Invented** — TC-RET-0345 row in canonical HTML has different title (truncated in source)                                                                                                                               |
| Auth & Session    | `"Re-auth challenge on refund completion above $5000"` / `"14.2s"`               | **Invented** (TC-AUT-0094 canonical title not extracted; defaulted to canonical-likely)                                                                                                                                  |
| Auth & Session    | `"Session persistence across tab close + reopen"` / `"9.7s"`                     | **Invented** (TC-AUT-0102 canonical title not extracted)                                                                                                                                                                 |
| Payments & Tender | `"Multi-currency reversal — INR cart, USD refund link"` / `"11.8s"`              | **Invented** (TC-PAY-0218 canonical title not visible in my extraction)                                                                                                                                                  |
| Payments & Tender | `"Refund callback receives gateway-required field"` / `"7.8s"`                   | **Invented** (TC-PAY-0224 canonical title not visible)                                                                                                                                                                   |

### Invented page-level wrapper text (3 violations)

These are React-only chrome I added — they don't exist anywhere in the canonical HTML:

| String                                                                                                                      | Where in my code                                                                             |
| --------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `"Failure clusters"`                                                                                                        | Eyebrow above cluster cards (`F20_FAILURE_CLUSTERS_EYEBROW`) — page-level decoration I added |
| `"3 distinct failure modes detected by Sherlock RCA. Open a defect from any cluster (Sherlock pre-fills the description)."` | Intro paragraph below eyebrow (`F20_FAILURE_CLUSTERS_INTRO`) — pure invention                |

### Run-level action button labels (3 violations)

| String                   | Notes                                                                      |
| ------------------------ | -------------------------------------------------------------------------- |
| `"Export results (CSV)"` | Likely IN canonical (L580+ run-level actions), need exact string verify    |
| `"Re-run failed only"`   | Likely IN canonical with count appended; my code adds `" (23)"` separately |
| `"Share this run"`       | Likely IN canonical                                                        |

### Formatting drift (2 violations)

| String    | Drift                                                                                                                                      |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `"8/ 23"` | Canonical has `"8 / 23"` (space before slash) per cluster cl-count format. I used `"8/ 23"` (no space before, space after). Minor cosmetic |
| `"10.6%"` | Canonical likely renders this in stat — need to verify spacing/exact                                                                       |

### Auth-suite case 1 title

Actually verified — `"Refund webhook receives refund.retry.exhausted — handler timeout"` appears in F19 HTML (selected case panel) but NOT F20 HTML. I cross-frame-cited as the F20 TC-RET-0342 title which matches conceptually but the EXACT string in F20 HTML's case row is different (likely truncated form).

**F20 verdict:** 6 invented case-row titles + 2 page-wrapper strings + 2 formatting drifts = **real Hard Rule 17 violations**. The 3 run-level action labels and the 1 cross-frame title may be near-verbatim and just need exact-spelling alignment.

## F21 violations (2)

| #   | String                                                                                                                    | Notes                                                                                                                                                                                     |
| --- | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `"Search defects, IDs, JIRA, comments…"`                                                                                  | Search placeholder I added. Canonical search field has placeholder but exact text not extracted — likely close paraphrase or invention                                                    |
| 2   | `" sets a 30 s read-timeout on the inbound socket. When the partner-bank gateway..."` (long Sherlock RCA second-sentence) | This is the F21_SHERLOCK_RCA.text segment middle portion. The string IS in canonical L≈1195 but split with whitespace/entity differences. Likely false positive due to grep normalization |

**F21 verdict:** ≤ 1 real violation (search placeholder). Cleanest of the three because Hard Rule 17 was applied from start.

## Cross-frame trend

- **F21 (Day-18, Hard Rule 17 from start)** — 1.1 % violation rate
- **F19 (Day-17, pre-Rule-17)** — 4.7 % violation rate, mostly false positives
- **F20 (Day-18 PM, Hard Rule 17 fix-forward of PR #145)** — **10.1 %** violation rate

The F20 fix-forward (PR #150) was still partially invention-prone because I extracted the v2 HTML's structural elements verbatim but **padded case rows** beyond what the canonical HTML actually contained, and added page-level wrapper text (eyebrow + intro) that has no canonical counterpart.

## What this audit implies for Day-20+ re-port

When the new `.claude/skills/frame-port/` + `apps/web/tests/visual/` toolchain lands:

1. **F20 re-port priority** — 8 case-row title/duration corrections + 2 invented wrapper strings to remove. ~30 min of cleanup.
2. **F21 re-port** — only needs canonical search placeholder fetch; rest passes audit.
3. **F19 re-port** — false-positive cleanup (whitelist enum keys + JSDoc); ≤ 1 real string to verify (cross-frame leak in comments).

## Action

**Per instruction: violations enumerated, NOT fixed.** Resume Day-20 10 AM with new toolchain. F21 wip branch unchanged. F20 PR #150 stays held (will be revised or superseded post-toolchain). F19 stays on main.
